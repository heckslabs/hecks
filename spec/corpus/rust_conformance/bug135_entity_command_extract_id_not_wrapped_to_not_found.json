{
  "domain": "examples/roster",
  "note": "BUG#135 (QualityControl ledger, hecks_qa sweep SW-roster-1789347275) — Roster::Roster.Member.Retire called with genuinely empty args ({}) diverged: Ruby refused NotFound (\"Retire acts on a Roster's Member — pass name.value:\", EntityInterpreter#parent's entity_parent_no_identity wrap — entity_interpreter.rb), the compiled Rust conformance binary refused TypeMismatch (\"Roster: no identity found (tried name.value, id, roster)\") instead. Member.Retire declares its own `id` attribute `optional: true` (examples/roster/bluebook/roster.bluebook — not a routing source; an entity's own identity is always read off extract_id/route, never this optional echo argument), so registry.rb/registry.rs's own structural_precheck_line (unknown/absent-argument names only) never fires for this shape and BOTH the parent's and the entity's own extract_id(facts_json) calls, inside the route-less entity_arms/nested_entity_arms None branch, ran completely unguarded — a bare `?` let extract_id's own raw TypeMismatch propagate directly, never wrapped into NotFound the way the AGGREGATE arm's own id_line already does (BUG#20). A DIFFERENT root cause than BUG#38/#126 (both about structural_precheck_line's own PLACEMENT relative to extract_id, an argument-existence-check ordering issue that never applies here since nothing IS unknown or absent-required for this optional-id command) — roster's committed Rust output was not stale (byte-identical to bin/project_rust examples/roster's own output on this commit; roster is already in .github/workflows/ci-checks.yml's regen list). Fixed by wrapping every extract_id(facts_json) call inside registry.rb's/registry.rs's route-less entity_arms and nested_entity_arms None branches with .map_err(|_| NotFound(...)), rendering the exact entity_parent_no_identity/entity_element_no_identity wording Ruby's own EntityInterpreter#parent/EntityElement#element_of produce, in both codegen sources (rust/project/registry.rb, rust/codegen/src/registry.rs) plus every already-generated in-repo domain regenerated via the full CI regen sequence.",
  "steps": [
    {"verb": "Roster::Roster.Member.Retire", "args": {}}
  ]
}
