// GENERATED by bin/project_refusal_wording from Hecks::Runtime::
// RefusalWording::TEMPLATES (lib/hecks/runtime/refusal_wording.rb).
// Do not hand-edit — re-run bin/project_refusal_wording instead.
//
// ONE VARIANT PER (refusal class, site) PAIR — 44 entries, every
// one Ruby's own table declares, not a subset picked for whichever Rust
// call site happens to exist today. See bin/project_refusal_wording's
// own header for the full argument (ground truth, why the whole table,
// why this isn't an extension of bin/project_kernel_capabilities); the
// short version is that a refusal template's text IS its behaviour, so
// there is no hand-written interpretation file for a generator to pair
// this roster with the way attribute_shapes/expression_operators need.
//
// `render` is `RefusalWording.render` (refusal_wording.rb:103-109),
// ported directly: a plain left-to-right `{key}` replace-fold over the
// template text, never expression syntax — a template is READ, not
// evaluated. Every real Ruby call site already pre-stringifies whatever
// isn't already a plain scalar (a joined list, `.inspect`, a rendered
// identity reading) before calling `.render`, so `&[(&str, &str)]` is
// sufficient here too — nothing generated ever needs to hand this a
// richer value and let it stringify internally.
//
// `Refusal::X(...)` CLASS selection and `RefusalSite` TEMPLATE selection
// stay two independent decisions at every call site, exactly mirroring
// Ruby's own `raise(Class, RefusalWording.render(...))` — two separate
// words, chosen separately, never fused into one Rust-only enum that
// would invent a coupling neither the language nor the runtime commits
// to.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum RefusalSite {
    NotFoundCreatingNoIdentity,
    AlreadyExistsCreatingDuplicate,
    AlreadyExistsEntityDuplicate,
    NotFoundActingNoIdentity,
    NotFoundRecordMissing,
    NotFoundEntityParentNoIdentity,
    UnknownVerbEntityUnknown,
    NotFoundEntityElementNoIdentity,
    NotFoundEntityElementMissing,
    NotFoundReferenceTargetMissing,
    NotFoundReadModelReferenceMissing,
    TypeMismatchReadModelObjectReference,
    UnknownVerbNoQuery,
    UnknownVerbEntityQueryMissing,
    UnknownVerbEntityHoldsNoList,
    UnknownVerbEntityNoCommand,
    UnknownVerbAggregateNoCommand,
    UnknownVerbPortNoOperation,
    UnknownVerbNoDomain,
    UnknownVerbNoReadModel,
    UnknownVerbNotFullyQualified,
    UnknownVerbNoAggregate,
    LifecycleRefusedTransitionBlocked,
    TypeMismatchValueObjectShape,
    TypeMismatchReferenceAsObject,
    TypeMismatchMultiFieldScalar,
    TypeMismatchCompositeIdentity,
    TypeMismatchNumericField,
    TypeMismatchNonFiniteField,
    TypeMismatchIntegerRange,
    TypeMismatchPatternMismatch,
    TypeMismatchArithmeticAmount,
    TypeMismatchArithmeticCurrent,
    TypeMismatchArithmeticSharedField,
    UnknownArgumentUnknownArgs,
    AbsentArgumentAbsentArgs,
    InvariantViolationClosedSetMember,
    InvariantViolationValueObjectInvariant,
    InvariantViolationAdmitsDeclaredSet,
    InvariantViolationUndeclaredSet,
    UnauthorizedTenantRequired,
    UnauthorizedRoleMismatch,
    AttributeAbsentAbsentRead,
    ProjectionAbsentAbsentRead,
}

impl RefusalSite {
    /// The template TEXT, read directly off Ruby's own table — see this
    /// file's own header for the ground-truth citation. Exhaustive, no
    /// wildcard arm, the same discipline `AttributeShape`/
    /// `OperatorCategory` (rust/src/kernel/{attribute_shapes,
    /// expression_operators}/mod.rs) already hold themselves to: a new
    /// entry Ruby's table gains and this generator hasn't re-run for
    /// yet is a compile error somewhere else in the crate, never a
    /// silently-wrong string.
    pub fn template(&self) -> &'static str {
        match self {
            RefusalSite::NotFoundCreatingNoIdentity => "{command} creates a {aggregate} — pass {identity}:",
            RefusalSite::AlreadyExistsCreatingDuplicate => "{command} creates a {aggregate} that already exists — {identity} {offered}",
            RefusalSite::AlreadyExistsEntityDuplicate => "a {entity} already exists on {aggregate} — {identity} {offered}",
            RefusalSite::NotFoundActingNoIdentity => "{command} acts on an existing {aggregate} — pass {identity}:",
            RefusalSite::NotFoundRecordMissing => "no {aggregate} with {identity} {offered}",
            RefusalSite::NotFoundEntityParentNoIdentity => "{command} acts on a {aggregate}'s {entity} — pass {identity}:",
            RefusalSite::UnknownVerbEntityUnknown => "{aggregate} has no entity {entity}",
            RefusalSite::NotFoundEntityElementNoIdentity => "{command} acts on one {entity} — pass {identity}:",
            RefusalSite::NotFoundEntityElementMissing => "no {entity} with {identity} {wants} on {aggregate} {parent_id}",
            RefusalSite::NotFoundReferenceTargetMissing => "no {target} with {heads} {key}",
            RefusalSite::NotFoundReadModelReferenceMissing => "no {aggregate} with reference {offered}",
            RefusalSite::TypeMismatchReadModelObjectReference => "{query} refused — a reference is an id, and {field} arrived as an object",
            RefusalSite::UnknownVerbNoQuery => "{aggregate} has no query {query}",
            RefusalSite::UnknownVerbEntityQueryMissing => "{entity} has no query {query}",
            RefusalSite::UnknownVerbEntityHoldsNoList => "{aggregate} holds no list of {entity}",
            RefusalSite::UnknownVerbEntityNoCommand => "{entity} has no command {command}",
            RefusalSite::UnknownVerbAggregateNoCommand => "{aggregate} has no command {command}",
            RefusalSite::UnknownVerbPortNoOperation => "{port} has no operation {operation}",
            RefusalSite::UnknownVerbNoDomain => "no domain {domain} loaded (verb {verb})",
            RefusalSite::UnknownVerbNoReadModel => "{domain} has no read model {query}",
            RefusalSite::UnknownVerbNotFullyQualified => "{verb} is not a fully-qualified verb (Domain::Aggregate.Command)",
            RefusalSite::UnknownVerbNoAggregate => "{domain} has no aggregate {aggregate}",
            RefusalSite::LifecycleRefusedTransitionBlocked => "{command} refused — {field} is {current}, and {command} moves it only from {allowed}",
            RefusalSite::TypeMismatchValueObjectShape => "{name} is a {type} — pass its fields as an object, not {offered}",
            RefusalSite::TypeMismatchReferenceAsObject => "{command} refused — a reference is an id, and {attribute} arrived as an object{known_by}",
            RefusalSite::TypeMismatchMultiFieldScalar => "{type} has multiple fields and cannot stand in for a scalar",
            RefusalSite::TypeMismatchCompositeIdentity => "{type} is a composite identity — an identity must have exactly one field",
            RefusalSite::TypeMismatchNumericField => "{type}.{field} expects {expected}, got {offered}",
            RefusalSite::TypeMismatchNonFiniteField => "{type}.{field} must be a finite number, got {offered}",
            RefusalSite::TypeMismatchIntegerRange => "{type}.{field} must fit in a 64-bit integer, got {offered}",
            RefusalSite::TypeMismatchPatternMismatch => "{type}.{field} must match {pattern}, got {offered}",
            RefusalSite::TypeMismatchArithmeticAmount => "{op} of {target} needs an Integer, got {offered}",
            RefusalSite::TypeMismatchArithmeticCurrent => "{op} of {target} needs an Integer {target}, got {offered}",
            RefusalSite::TypeMismatchArithmeticSharedField => "{op} of {target} needs a value object with one shared Integer field",
            RefusalSite::UnknownArgumentUnknownArgs => "{command} does not declare {unknown} — it takes {declared}",
            RefusalSite::AbsentArgumentAbsentArgs => "{command} was not given {absent} — it takes {declared}",
            RefusalSite::InvariantViolationClosedSetMember => "{type} admits {admitted} — got {offered}",
            RefusalSite::InvariantViolationValueObjectInvariant => "{name} invariant violated — {description} (given {offered})",
            RefusalSite::InvariantViolationAdmitsDeclaredSet => "{name} admits {admits} — {admitted} — got {offered}",
            RefusalSite::InvariantViolationUndeclaredSet => "{name} admits {admits}, which this chapter does not declare — a closed set is named Aggregate::SetName, and it must be one the bluebook actually holds",
            RefusalSite::UnauthorizedTenantRequired => "{query} declares authorize with tenant: {field} — pass {field}: to name which {field} this ask is scoped to",
            RefusalSite::UnauthorizedRoleMismatch => "{command} refused — role: {role}, and the caller stated {caller_role}",
            RefusalSite::AttributeAbsentAbsentRead => "{aggregate} {field} is absent on this record — declared, not optional, and added since it was written. Backfill it in a translation (backfill :{field}, default: ...), or declare it optional: true",
            RefusalSite::ProjectionAbsentAbsentRead => "{aggregate} {field} is not yet projected on this record — declared via projects :{field}, but no rebuild sweep has populated it. Run the sweep, or read {reference}.{remote_field} directly if this rule cannot wait",
        }
    }

    /// `RefusalWording.render`, read directly: `values.reduce(template)
    /// { |text, (key, value)| text.gsub("{#{key}}", value.to_s) }`.
    /// `String::replace` matches `gsub` here — both replace EVERY
    /// occurrence of the marker, not just the first — and folding over
    /// `values` in the caller-supplied order matches Ruby's own
    /// insertion-ordered `**values` Hash iteration. A caller passes
    /// only the placeholders ITS OWN template actually names; an unused
    /// pair is harmless (its `{key}` marker simply never appears to
    /// replace), the same as Ruby's version.
    pub fn render(&self, values: &[(&str, &str)]) -> String {
        let mut text = self.template().to_string();
        for (key, value) in values {
            text = text.replace(&format!("{{{key}}}"), value);
        }
        text
    }

    /// A verification sweep — every real generated call site names one
    /// exact variant directly, so nothing in this crate iterates `ALL`
    /// for dispatch; it exists for the `#[cfg(test)]` block below (and
    /// any future one) to walk the whole table generically.
    pub const ALL: &'static [RefusalSite] = &[RefusalSite::NotFoundCreatingNoIdentity, RefusalSite::AlreadyExistsCreatingDuplicate, RefusalSite::AlreadyExistsEntityDuplicate, RefusalSite::NotFoundActingNoIdentity, RefusalSite::NotFoundRecordMissing, RefusalSite::NotFoundEntityParentNoIdentity, RefusalSite::UnknownVerbEntityUnknown, RefusalSite::NotFoundEntityElementNoIdentity, RefusalSite::NotFoundEntityElementMissing, RefusalSite::NotFoundReferenceTargetMissing, RefusalSite::NotFoundReadModelReferenceMissing, RefusalSite::TypeMismatchReadModelObjectReference, RefusalSite::UnknownVerbNoQuery, RefusalSite::UnknownVerbEntityQueryMissing, RefusalSite::UnknownVerbEntityHoldsNoList, RefusalSite::UnknownVerbEntityNoCommand, RefusalSite::UnknownVerbAggregateNoCommand, RefusalSite::UnknownVerbPortNoOperation, RefusalSite::UnknownVerbNoDomain, RefusalSite::UnknownVerbNoReadModel, RefusalSite::UnknownVerbNotFullyQualified, RefusalSite::UnknownVerbNoAggregate, RefusalSite::LifecycleRefusedTransitionBlocked, RefusalSite::TypeMismatchValueObjectShape, RefusalSite::TypeMismatchReferenceAsObject, RefusalSite::TypeMismatchMultiFieldScalar, RefusalSite::TypeMismatchCompositeIdentity, RefusalSite::TypeMismatchNumericField, RefusalSite::TypeMismatchNonFiniteField, RefusalSite::TypeMismatchIntegerRange, RefusalSite::TypeMismatchPatternMismatch, RefusalSite::TypeMismatchArithmeticAmount, RefusalSite::TypeMismatchArithmeticCurrent, RefusalSite::TypeMismatchArithmeticSharedField, RefusalSite::UnknownArgumentUnknownArgs, RefusalSite::AbsentArgumentAbsentArgs, RefusalSite::InvariantViolationClosedSetMember, RefusalSite::InvariantViolationValueObjectInvariant, RefusalSite::InvariantViolationAdmitsDeclaredSet, RefusalSite::InvariantViolationUndeclaredSet, RefusalSite::UnauthorizedTenantRequired, RefusalSite::UnauthorizedRoleMismatch, RefusalSite::AttributeAbsentAbsentRead, RefusalSite::ProjectionAbsentAbsentRead];
}

#[cfg(test)]
mod tests {
    use super::*;

    // Every template renders clean — no leftover `{...}` marker — when
    // handed a dummy value for each placeholder ITS OWN text names.
    // Placeholders are discovered by scanning the template text itself
    // (never a second, hand-maintained placeholder list that could
    // drift from the templates above), so this stays a true
    // whole-table sweep no matter how the table above is edited by a
    // future regeneration.
    #[test]
    fn every_site_renders_with_no_leftover_placeholder() {
        for site in RefusalSite::ALL {
            let template = site.template();
            let mut keys: Vec<String> = Vec::new();
            let mut inside = false;
            let mut current = String::new();
            for ch in template.chars() {
                match ch {
                    '{' => {
                        inside = true;
                        current.clear();
                    }
                    '}' if inside => {
                        inside = false;
                        keys.push(current.clone());
                    }
                    _ if inside => current.push(ch),
                    _ => {}
                }
            }

            let dummies: Vec<(String, String)> = keys.iter().map(|k| (k.clone(), format!("<{k}>"))).collect();
            let pairs: Vec<(&str, &str)> = dummies.iter().map(|(k, v)| (k.as_str(), v.as_str())).collect();
            let rendered = site.render(&pairs);

            assert!(
                !rendered.contains('{') && !rendered.contains('}'),
                "{site:?} left an unrendered placeholder behind: {rendered:?}"
            );
        }
    }
}
