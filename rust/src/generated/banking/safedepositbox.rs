// GENERATED by bin/project_rust from examples/banking's canonical IR.
// Do not hand-edit — re-run bin/project_rust instead.
#![allow(dead_code, unused_variables)]
use crate::kernel::Expr;

#[derive(Debug, Clone, PartialEq)]
pub struct BranchCode {
    pub value: String,
}

impl crate::kernel::Fielded for BranchCode {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "value" => Some(Field::Value(Value::Str(self.value.clone()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        match self.field("value") { Some(crate::kernel::Field::Value(v)) => Some(v), _ => None }
    }
}


impl BranchCode {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {
        if !crate::kernel::pattern::matches("[^ \\t\\n\\r]", &self.value) { return Err(crate::kernel::Refusal::TypeMismatch(format!("{}{:?}", "BranchCode.value must match [^ \\t\\n\\r], got ", self.value))); }
{
    let ctx = crate::kernel::EvalContext { args: &crate::kernel::NoFields, instance: self };
    if !crate::kernel::interpret(&Expr::Not(Box::new(Expr::Empty(Box::new(Expr::ToS(Box::new(Expr::Lookup("value"))))))), &ctx)?.truthy() {
        let mut offered = self.to_json();
        if let crate::kernel::Json::Object(fields) = &mut offered {
            fields.sort_by(|a, b| a.0.cmp(&b.0));
        }
        let offered = offered.to_json_string();
        return Err(crate::kernel::Refusal::InvariantViolation(crate::kernel::RefusalSite::InvariantViolationValueObjectInvariant.render(&[
            ("name", "BranchCode"),
            ("description", "a branch is coded"),
            ("offered", offered.as_str()),
        ])));
    }
}
        Ok(())
    }
}

impl BranchCode {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("value".to_string(), crate::kernel::Json::Str(self.value.clone())),
        ])
    }
}

impl BranchCode {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("BranchCode expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["value"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "BranchCode does not declare {} — it takes value",
        unknown.join(", ")
    )));
}
        Ok(Self {
        value: { let x = v.get("value").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("BranchCode.value expects String, got nil".to_string()))?; x.as_str().map(|s| s.to_string()).ok_or_else(|| if matches!(x, crate::kernel::Json::Array(_) | crate::kernel::Json::Object(_) | crate::kernel::Json::Null) { crate::kernel::Refusal::TypeMismatch(format!("BranchCode.value expects String, got {}", x.inspect())) } else { crate::kernel::Refusal::TypeMismatch("BranchCode.value: expected String".to_string()) })? },
        })
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct BoxNumber {
    pub value: i64,
}

impl crate::kernel::Fielded for BoxNumber {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "value" => Some(Field::Value(Value::Int(self.value))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        match self.field("value") { Some(crate::kernel::Field::Value(v)) => Some(v), _ => None }
    }
}


impl BoxNumber {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {
{
    let ctx = crate::kernel::EvalContext { args: &crate::kernel::NoFields, instance: self };
    if !crate::kernel::interpret(&Expr::SignTest { op: crate::kernel::Comparison { less_than: true, equal: true, negated: true }, receiver: Box::new(Expr::Lookup("value")) }, &ctx)?.truthy() {
        let mut offered = self.to_json();
        if let crate::kernel::Json::Object(fields) = &mut offered {
            fields.sort_by(|a, b| a.0.cmp(&b.0));
        }
        let offered = offered.to_json_string();
        return Err(crate::kernel::Refusal::InvariantViolation(crate::kernel::RefusalSite::InvariantViolationValueObjectInvariant.render(&[
            ("name", "BoxNumber"),
            ("description", "a box is numbered from one"),
            ("offered", offered.as_str()),
        ])));
    }
}
        Ok(())
    }
}

impl BoxNumber {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("value".to_string(), crate::kernel::Json::int(self.value)),
        ])
    }
}

impl BoxNumber {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("BoxNumber expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["value"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "BoxNumber does not declare {} — it takes value",
        unknown.join(", ")
    )));
}
        Ok(Self {
        value: { let x = v.get("value").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("BoxNumber.value expects Integer, got nil".to_string()))?; x.as_i64().ok_or_else(|| crate::kernel::Refusal::TypeMismatch(format!("BoxNumber.value expects Integer, got {}", x.inspect())))? },
        })
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct VisitSequence {
    pub value: i64,
}

impl crate::kernel::Fielded for VisitSequence {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "value" => Some(Field::Value(Value::Int(self.value))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        match self.field("value") { Some(crate::kernel::Field::Value(v)) => Some(v), _ => None }
    }
}


impl VisitSequence {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {
{
    let ctx = crate::kernel::EvalContext { args: &crate::kernel::NoFields, instance: self };
    if !crate::kernel::interpret(&Expr::SignTest { op: crate::kernel::Comparison { less_than: true, equal: true, negated: true }, receiver: Box::new(Expr::Lookup("value")) }, &ctx)?.truthy() {
        let mut offered = self.to_json();
        if let crate::kernel::Json::Object(fields) = &mut offered {
            fields.sort_by(|a, b| a.0.cmp(&b.0));
        }
        let offered = offered.to_json_string();
        return Err(crate::kernel::Refusal::InvariantViolation(crate::kernel::RefusalSite::InvariantViolationValueObjectInvariant.render(&[
            ("name", "VisitSequence"),
            ("description", "a visit sequence is positive"),
            ("offered", offered.as_str()),
        ])));
    }
}
        Ok(())
    }
}

impl VisitSequence {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("value".to_string(), crate::kernel::Json::int(self.value)),
        ])
    }
}

impl VisitSequence {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("VisitSequence expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["value"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "VisitSequence does not declare {} — it takes value",
        unknown.join(", ")
    )));
}
        Ok(Self {
        value: { let x = v.get("value").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("VisitSequence.value expects Integer, got nil".to_string()))?; x.as_i64().ok_or_else(|| crate::kernel::Refusal::TypeMismatch(format!("VisitSequence.value expects Integer, got {}", x.inspect())))? },
        })
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct VisitDate {
    pub value: String,
}

impl crate::kernel::Fielded for VisitDate {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "value" => Some(Field::Value(Value::Str(self.value.clone()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        match self.field("value") { Some(crate::kernel::Field::Value(v)) => Some(v), _ => None }
    }
}


impl VisitDate {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {
        if !crate::kernel::pattern::matches("[^ \\t\\n\\r]", &self.value) { return Err(crate::kernel::Refusal::TypeMismatch(format!("{}{:?}", "VisitDate.value must match [^ \\t\\n\\r], got ", self.value))); }
{
    let ctx = crate::kernel::EvalContext { args: &crate::kernel::NoFields, instance: self };
    if !crate::kernel::interpret(&Expr::Not(Box::new(Expr::Empty(Box::new(Expr::ToS(Box::new(Expr::Lookup("value"))))))), &ctx)?.truthy() {
        let mut offered = self.to_json();
        if let crate::kernel::Json::Object(fields) = &mut offered {
            fields.sort_by(|a, b| a.0.cmp(&b.0));
        }
        let offered = offered.to_json_string();
        return Err(crate::kernel::Refusal::InvariantViolation(crate::kernel::RefusalSite::InvariantViolationValueObjectInvariant.render(&[
            ("name", "VisitDate"),
            ("description", "a visit names its date"),
            ("offered", offered.as_str()),
        ])));
    }
}
        Ok(())
    }
}

impl VisitDate {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("value".to_string(), crate::kernel::Json::Str(self.value.clone())),
        ])
    }
}

impl VisitDate {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("VisitDate expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["value"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "VisitDate does not declare {} — it takes value",
        unknown.join(", ")
    )));
}
        Ok(Self {
        value: { let x = v.get("value").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("VisitDate.value expects String, got nil".to_string()))?; x.as_str().map(|s| s.to_string()).ok_or_else(|| if matches!(x, crate::kernel::Json::Array(_) | crate::kernel::Json::Object(_) | crate::kernel::Json::Null) { crate::kernel::Refusal::TypeMismatch(format!("VisitDate.value expects String, got {}", x.inspect())) } else { crate::kernel::Refusal::TypeMismatch("VisitDate.value: expected String".to_string()) })? },
        })
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct VisitNote {
    pub text: String,
}

impl crate::kernel::Fielded for VisitNote {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "text" => Some(Field::Value(Value::Str(self.text.clone()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        match self.field("text") { Some(crate::kernel::Field::Value(v)) => Some(v), _ => None }
    }
}


impl VisitNote {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {

        Ok(())
    }
}

impl VisitNote {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("text".to_string(), crate::kernel::Json::Str(self.text.clone())),
        ])
    }
}

impl VisitNote {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("VisitNote expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["text"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "VisitNote does not declare {} — it takes text",
        unknown.join(", ")
    )));
}
        Ok(Self {
        text: { let x = v.get("text").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("VisitNote.text expects String, got nil".to_string()))?; x.as_str().map(|s| s.to_string()).ok_or_else(|| if matches!(x, crate::kernel::Json::Array(_) | crate::kernel::Json::Object(_) | crate::kernel::Json::Null) { crate::kernel::Refusal::TypeMismatch(format!("VisitNote.text expects String, got {}", x.inspect())) } else { crate::kernel::Refusal::TypeMismatch("VisitNote.text: expected String".to_string()) })? },
        })
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct KeySerial {
    pub value: String,
}

impl crate::kernel::Fielded for KeySerial {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "value" => Some(Field::Value(Value::Str(self.value.clone()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        match self.field("value") { Some(crate::kernel::Field::Value(v)) => Some(v), _ => None }
    }
}


impl KeySerial {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {
        if !crate::kernel::pattern::matches("[^ \\t\\n\\r]", &self.value) { return Err(crate::kernel::Refusal::TypeMismatch(format!("{}{:?}", "KeySerial.value must match [^ \\t\\n\\r], got ", self.value))); }
{
    let ctx = crate::kernel::EvalContext { args: &crate::kernel::NoFields, instance: self };
    if !crate::kernel::interpret(&Expr::Not(Box::new(Expr::Empty(Box::new(Expr::ToS(Box::new(Expr::Lookup("value"))))))), &ctx)?.truthy() {
        let mut offered = self.to_json();
        if let crate::kernel::Json::Object(fields) = &mut offered {
            fields.sort_by(|a, b| a.0.cmp(&b.0));
        }
        let offered = offered.to_json_string();
        return Err(crate::kernel::Refusal::InvariantViolation(crate::kernel::RefusalSite::InvariantViolationValueObjectInvariant.render(&[
            ("name", "KeySerial"),
            ("description", "a key is serialed"),
            ("offered", offered.as_str()),
        ])));
    }
}
        Ok(())
    }
}

impl KeySerial {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("value".to_string(), crate::kernel::Json::Str(self.value.clone())),
        ])
    }
}

impl KeySerial {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("KeySerial expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["value"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "KeySerial does not declare {} — it takes value",
        unknown.join(", ")
    )));
}
        Ok(Self {
        value: { let x = v.get("value").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("KeySerial.value expects String, got nil".to_string()))?; x.as_str().map(|s| s.to_string()).ok_or_else(|| if matches!(x, crate::kernel::Json::Array(_) | crate::kernel::Json::Object(_) | crate::kernel::Json::Null) { crate::kernel::Refusal::TypeMismatch(format!("KeySerial.value expects String, got {}", x.inspect())) } else { crate::kernel::Refusal::TypeMismatch("KeySerial.value: expected String".to_string()) })? },
        })
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Size {
    Small,
    Medium,
    Large,
}

impl crate::kernel::Fielded for Size {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::{Field, Value};
        match name {
            "value" => Some(Field::Value(Value::Str(match self { Size::Small => "small".to_string(), Size::Medium => "medium".to_string(), Size::Large => "large".to_string(), }))),
            _ => None,
        }
    }
    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        match self.field("value") { Some(crate::kernel::Field::Value(v)) => Some(v), _ => None }
    }
}

impl Size {
    pub fn to_json(&self) -> crate::kernel::Json {
        let member = match self {
            Size::Small => "small",
            Size::Medium => "medium",
            Size::Large => "large",
        };
        crate::kernel::Json::obj(vec![("value", crate::kernel::Json::str(member))])
    }

    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
        let raw = v.require("value", "Size")?.as_str()
            .ok_or_else(|| crate::kernel::Refusal::TypeMismatch("Size.value: expected string".to_string()))?;
        match raw {
            "small" => Ok(Size::Small),
            "medium" => Ok(Size::Medium),
            "large" => Ok(Size::Large),
            other => Err(crate::kernel::Refusal::InvariantViolation(crate::kernel::RefusalSite::InvariantViolationClosedSetMember.render(&[
                ("type", "Size"),
                ("admitted", "\"small\", \"medium\", \"large\""),
                ("offered", &format!("{:?}", other)),
            ]))),
        }
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Visit {
    pub date: VisitDate,
    pub sequence: VisitSequence,
    pub note: Option<VisitNote>,
    pub state: String,
}

impl crate::kernel::Fielded for Visit {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "date" => Some(Field::Nested(&self.date)),
            "sequence" => Some(Field::Nested(&self.sequence)),
            "note" => self.note.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            "state" => Some(Field::Value(Value::Str(self.state.clone()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}

impl Visit {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("date".to_string(), self.date.to_json()),
        ("sequence".to_string(), self.sequence.to_json()),
        ("note".to_string(), self.note.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),
        ("state".to_string(), crate::kernel::Json::Str(self.state.clone())),
        ])
    }
}

impl Visit {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("Visit expects an object, got {}", v.inspect())));
}
        Ok(Self {
        date: VisitDate::from_json(&v.require("date", "Visit")?.coerce_single_field("value"))?,
        sequence: VisitSequence::from_json(&v.require("sequence", "Visit")?.coerce_single_field("value"))?,
        note: match v.get("note") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(VisitNote::from_json(&x.coerce_single_field("text"))?), },
        state: v.require("state", "Visit")?.as_str().ok_or_else(|| crate::kernel::Refusal::TypeMismatch("Visit.state: expected a string".to_string()))?.to_string(),
        })
    }
}

impl Visit {
    pub fn extract_id(v: &crate::kernel::Json) -> Result<String, crate::kernel::Refusal> {
        let by_identity = (|| -> Option<String> {
            let c0 = v.dig("date.value")?.to_id_component().ok()?;
            let c1 = v.dig("sequence.value")?.to_id_component().ok()?;
            Some(vec![c0, c1].join(":"))
        })();
        let by_id_key = v.get("id").and_then(|j| j.to_id_component().ok());
        let by_reference_key = v.get("visit").and_then(|j| j.to_id_component().ok());

        by_identity.or(by_id_key).or(by_reference_key).ok_or_else(|| {
            crate::kernel::Refusal::TypeMismatch("Visit: no identity found (tried date.value, sequence.value, id, visit)".to_string())
        })
    }
}

impl Visit {
    pub fn extract_wants(v: &crate::kernel::Json) -> String {
        (|| -> Option<String> {
            let c0 = v.dig("date.value")?.to_id_component().ok()?;
            let c1 = v.dig("sequence.value")?.to_id_component().ok()?;
            Some(vec![c0, c1].join(", "))
        })()
        .unwrap_or_default()
    }
}

impl Visit {
    pub fn identity(&self) -> String {
        vec![self.date.value.to_string(), self.sequence.value.to_string()].join(":")
    }
}

impl crate::kernel::Fielded for VisitAnnotateEntityArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {
            "note" => Some(Field::Nested(&self.note)),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct VisitAnnotateEntityArgs {
    pub note: VisitNote,
}

impl VisitAnnotateEntityArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![        ("note".to_string(), self.note.to_json()),]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}


impl VisitAnnotateEntityArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("VisitAnnotateEntityArgs expects an object, got {}", v.inspect())));
}
let absent: Vec<&str> = ["note"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "Annotate"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "note"),
    ])));
}
        let note = VisitNote::from_json(&v.get("note").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("VisitAnnotateEntityArgs.note expects VisitNote, got nil".to_string()))?.coerce_single_field("text"))?;
        note.check_invariants()?;
        Ok(Self {
        note,
        })
    }
}


pub fn dispatch_entity_visit_annotate(
    repo: &mut impl crate::kernel::Repository<SafeDepositBox>, parent_id: &str, element_id: &str, element_wants: &str, args: VisitAnnotateEntityArgs,
    mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>,
) -> crate::kernel::DispatchResult<SafeDepositBox> {
        args.note.check_invariants()?;
    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, SAFE_DEPOSIT_BOX_PROJECTED_FIELDS);

    crate::kernel::dispatch_entity(
        repo,
        parent_id,
        |r: &SafeDepositBox| &r.visits,
        |r: &mut SafeDepositBox| &mut r.visits,
        |el: &Visit| el.identity() == element_id,
        "Annotate",
        "Banking::SafeDepositBox",
        "SafeDepositBox",
        "branch_code.value, box_number.value",
        "Visit",
        "date.value, sequence.value",
        element_wants,
        &with_references,
        &[
            crate::kernel::GivenSpec { description: "customer is active", expr: Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: false }, left: Box::new(Expr::Lookup("parent.customer_status")), right: Box::new(Expr::Str("active".to_string())) }, corrects_event: None },
            crate::kernel::GivenSpec { description: "box is rented", expr: Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: false }, left: Box::new(Expr::Lookup("parent.status")), right: Box::new(Expr::Str("rented".to_string())) }, corrects_event: None },
        ],
        Some(crate::kernel::TransitionCheck { field: "state", from_states: &["logged"] }),
        |record| {
        record.note = Some(args.note.clone());
        record.state = "logged".to_string();
            Ok(())
        },
        &[

        ],
        &safedepositbox_invariants(),
        &["VisitAnnotated"],
        args.to_json(),
        mutations,
        seed_projections,
    )
}

#[derive(Debug, Clone, PartialEq)]
pub struct KeyIssuance {
    pub serial: KeySerial,
    pub status: String,
}

impl crate::kernel::Fielded for KeyIssuance {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "serial" => Some(Field::Nested(&self.serial)),
            "status" => Some(Field::Value(Value::Str(self.status.clone()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}

impl KeyIssuance {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("serial".to_string(), self.serial.to_json()),
        ("status".to_string(), crate::kernel::Json::Str(self.status.clone())),
        ])
    }
}

impl KeyIssuance {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("KeyIssuance expects an object, got {}", v.inspect())));
}
        Ok(Self {
        serial: KeySerial::from_json(&v.require("serial", "KeyIssuance")?.coerce_single_field("value"))?,
        status: v.require("status", "KeyIssuance")?.as_str().ok_or_else(|| crate::kernel::Refusal::TypeMismatch("KeyIssuance.status: expected a string".to_string()))?.to_string(),
        })
    }
}

impl KeyIssuance {
    pub fn extract_id(v: &crate::kernel::Json) -> Result<String, crate::kernel::Refusal> {
        let by_identity = (|| -> Option<String> {
            let c0 = v.dig("serial.value")?.to_id_component().ok()?;
            Some(c0)
        })();
        let by_id_key = v.get("id").and_then(|j| j.to_id_component().ok());
        let by_reference_key = v.get("key_issuance").and_then(|j| j.to_id_component().ok());

        by_identity.or(by_id_key).or(by_reference_key).ok_or_else(|| {
            crate::kernel::Refusal::TypeMismatch("KeyIssuance: no identity found (tried serial.value, id, key_issuance)".to_string())
        })
    }
}

impl KeyIssuance {
    pub fn extract_wants(v: &crate::kernel::Json) -> String {
        (|| -> Option<String> {
            let c0 = v.dig("serial.value")?.to_id_component().ok()?;
            Some(c0)
        })()
        .unwrap_or_default()
    }
}

impl KeyIssuance {
    pub fn identity(&self) -> String {
        self.serial.value.to_string()
    }
}

impl crate::kernel::Fielded for KeyIssuanceReturnEntityArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {
            "serial" => Some(Field::Nested(&self.serial)),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct KeyIssuanceReturnEntityArgs {
    pub serial: KeySerial,
}

impl KeyIssuanceReturnEntityArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![        ("serial".to_string(), self.serial.to_json()),]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}


impl KeyIssuanceReturnEntityArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("KeyIssuanceReturnEntityArgs expects an object, got {}", v.inspect())));
}
let absent: Vec<&str> = ["serial"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "Return"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "serial"),
    ])));
}
        let serial = KeySerial::from_json(&v.get("serial").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("KeyIssuanceReturnEntityArgs.serial expects KeySerial, got nil".to_string()))?.coerce_single_field("value"))?;
        serial.check_invariants()?;
        Ok(Self {
        serial,
        })
    }
}


pub fn dispatch_entity_keyissuance_return(
    repo: &mut impl crate::kernel::Repository<SafeDepositBox>, parent_id: &str, element_id: &str, element_wants: &str, args: KeyIssuanceReturnEntityArgs,
    mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>,
) -> crate::kernel::DispatchResult<SafeDepositBox> {
        args.serial.check_invariants()?;
    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, SAFE_DEPOSIT_BOX_PROJECTED_FIELDS);

    crate::kernel::dispatch_entity(
        repo,
        parent_id,
        |r: &SafeDepositBox| &r.keys,
        |r: &mut SafeDepositBox| &mut r.keys,
        |el: &KeyIssuance| el.identity() == element_id,
        "Return",
        "Banking::SafeDepositBox",
        "SafeDepositBox",
        "branch_code.value, box_number.value",
        "KeyIssuance",
        "serial.value",
        element_wants,
        &with_references,
        &[
            crate::kernel::GivenSpec { description: "customer is active", expr: Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: false }, left: Box::new(Expr::Lookup("parent.customer_status")), right: Box::new(Expr::Str("active".to_string())) }, corrects_event: None },
            crate::kernel::GivenSpec { description: "box is rented", expr: Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: false }, left: Box::new(Expr::Lookup("parent.status")), right: Box::new(Expr::Str("rented".to_string())) }, corrects_event: None },
        ],
        Some(crate::kernel::TransitionCheck { field: "status", from_states: &["issued"] }),
        |record| {
        record.status = "returned".to_string();
            Ok(())
        },
        &[

        ],
        &safedepositbox_invariants(),
        &["KeyReturned"],
        args.to_json(),
        mutations,
        seed_projections,
    )
}

#[derive(Debug, Clone, PartialEq)]
pub struct SafeDepositBox {
    pub customer: Option<String>,
    pub branch_code: Option<BranchCode>,
    pub box_number: Option<BoxNumber>,
    pub size: Option<Size>,
    pub visits: Vec<Visit>,
    pub keys: Vec<KeyIssuance>,
    pub customer_status: Option<String>,
    pub status: String,
}

impl crate::kernel::Fielded for SafeDepositBox {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::{Field, Value};
        match name {
            "customer" => self.customer.as_ref().map(|v| Field::Value(Value::Str(v.clone()))).or(Some(Field::Value(Value::Nil))),
            "branch_code" => self.branch_code.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            "box_number" => self.box_number.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            "size" => self.size.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            "visits" => Some(Field::Value(Value::List(self.visits.len()))),
            "keys" => Some(Field::Value(Value::List(self.keys.len()))),
            "customer_status" => self.customer_status.as_ref().map(|v| Field::Value(Value::Str(v.clone()))).or(Some(Field::Value(Value::Nil))),
            "status" => Some(Field::Value(Value::Str(self.status.clone()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {
            "visits" => Some(self.visits.iter().map(|v| Field::Nested(v)).collect()),
            "keys" => Some(self.keys.iter().map(|v| Field::Nested(v)).collect()),
            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}

impl SafeDepositBox {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("customer".to_string(), self.customer.as_ref().map(|v| crate::kernel::Json::Str(v.clone())).unwrap_or(crate::kernel::Json::Null)),
        ("branch_code".to_string(), self.branch_code.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),
        ("box_number".to_string(), self.box_number.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),
        ("size".to_string(), self.size.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),
        ("visits".to_string(), crate::kernel::Json::Array(self.visits.iter().map(|x| x.to_json()).collect())),
        ("keys".to_string(), crate::kernel::Json::Array(self.keys.iter().map(|x| x.to_json()).collect())),
        ("customer_status".to_string(), self.customer_status.as_ref().map(|v| crate::kernel::Json::Str(v.clone())).unwrap_or(crate::kernel::Json::Null)),
        ("status".to_string(), crate::kernel::Json::Str(self.status.clone())),
        ])
    }
}

impl SafeDepositBox {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("SafeDepositBox expects an object, got {}", v.inspect())));
}
        Ok(Self {
        customer: match v.get("customer") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(x.as_str().map(|s| s.to_string()).ok_or_else(|| if matches!(x, crate::kernel::Json::Array(_) | crate::kernel::Json::Object(_) | crate::kernel::Json::Null) { crate::kernel::Refusal::TypeMismatch(format!("SafeDepositBox.customer expects String, got {}", x.inspect())) } else { crate::kernel::Refusal::TypeMismatch("SafeDepositBox.customer: expected String".to_string()) })?), },
        branch_code: match v.get("branch_code") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(BranchCode::from_json(&x.coerce_single_field("value"))?), },
        box_number: match v.get("box_number") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(BoxNumber::from_json(&x.coerce_single_field("value"))?), },
        size: match v.get("size") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(Size::from_json(&x.coerce_single_field("value"))?), },
        visits: match v.get("visits").and_then(crate::kernel::Json::as_array) { Some(items) => items.iter().map(Visit::from_json).collect::<Result<Vec<_>, crate::kernel::Refusal>>()?, None => Vec::new(), },
        keys: match v.get("keys").and_then(crate::kernel::Json::as_array) { Some(items) => items.iter().map(KeyIssuance::from_json).collect::<Result<Vec<_>, crate::kernel::Refusal>>()?, None => Vec::new(), },
        customer_status: match v.get("customer_status") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(x.as_str().map(|s| s.to_string()).ok_or_else(|| if matches!(x, crate::kernel::Json::Array(_) | crate::kernel::Json::Object(_) | crate::kernel::Json::Null) { crate::kernel::Refusal::TypeMismatch(format!("SafeDepositBox.customer_status expects String, got {}", x.inspect())) } else { crate::kernel::Refusal::TypeMismatch("SafeDepositBox.customer_status: expected String".to_string()) })?), },
        status: v.require("status", "SafeDepositBox")?.as_str().ok_or_else(|| crate::kernel::Refusal::TypeMismatch("SafeDepositBox.status: expected a string".to_string()))?.to_string(),
        })
    }
}

impl crate::kernel::ToJson for SafeDepositBox {
    fn to_json(&self) -> crate::kernel::Json {
        SafeDepositBox::to_json(self)
    }
}

impl crate::kernel::SetProjectedField for SafeDepositBox {
    fn set_projected_field(&mut self, name: &'static str, value: Option<String>) {
        match name {
            "customer_status" => self.customer_status = value,
            _ => {}
        }
    }
}

pub static SAFE_DEPOSIT_BOX_PROJECTED_FIELDS: &[crate::kernel::ProjectedFieldSpec] = &[
    crate::kernel::ProjectedFieldSpec { field: "customer_status", reference: "customer", remote_field: "status" },
];

impl SafeDepositBox {
    pub fn extract_id(v: &crate::kernel::Json) -> Result<String, crate::kernel::Refusal> {
        let by_identity = (|| -> Option<String> {
            let c0 = v.dig("branch_code.value")?.to_id_component().ok()?;
            let c1 = v.dig("box_number.value")?.to_id_component().ok()?;
            Some(vec![c0, c1].join(":"))
        })();
        let by_id_key = v.get("id").and_then(|j| j.to_id_component().ok());
        let by_reference_key = v.get("safe_deposit_box").and_then(|j| j.to_id_component().ok());

        by_identity.or(by_id_key).or(by_reference_key).ok_or_else(|| {
            crate::kernel::Refusal::TypeMismatch("SafeDepositBox: no identity found (tried branch_code.value, box_number.value, id, safe_deposit_box)".to_string())
        })
    }
}

fn safedepositbox_invariants() -> crate::kernel::InvariantSet {
    use crate::kernel::Expr;
    crate::kernel::InvariantSet {
        aggregate: vec![],
        entities: vec![
            crate::kernel::EntityInvariants { name: "Visit", list_field: "visits", specs: vec![
                crate::kernel::InvariantSpec { description: "a written note is not blank", expr: Expr::Or(Box::new(Expr::Not(Box::new(Expr::Lookup("note")))), Box::new(Expr::Not(Box::new(Expr::Empty(Box::new(Expr::ToS(Box::new(Expr::Lookup("note.text"))))))))) },
            ], nested: vec![] },
        ],
    }
}

impl crate::kernel::Fielded for RentArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "branch_code" => Some(Field::Nested(&self.branch_code)),
            "box_number" => Some(Field::Nested(&self.box_number)),
            "size" => Some(Field::Nested(&self.size)),
            "customer" => Some(Field::Value(Value::Str(self.customer.clone()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct RentArgs {
    pub branch_code: BranchCode,
    pub box_number: BoxNumber,
    pub size: Size,
    pub customer: String,
}

pub fn dispatch_rent(
    repo: &mut impl crate::kernel::Repository<SafeDepositBox>, args: RentArgs, mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>,
) -> crate::kernel::DispatchResult<SafeDepositBox> {
        args.branch_code.check_invariants()?;
        args.box_number.check_invariants()?;
    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, SAFE_DEPOSIT_BOX_PROJECTED_FIELDS);

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Create {
        id: format!("{}:{}", args.branch_code.value.to_string(), args.box_number.value.to_string()),
        build: Box::new(|| SafeDepositBox {
            customer: Some(args.customer.clone()),
            branch_code: Some(args.branch_code.clone()),
            box_number: Some(args.box_number.clone()),
            size: Some(args.size.clone()),
            visits: vec![],
            keys: vec![],
            customer_status: None,
            status: "vacant".to_string(),
        }),
    },
        "Rent",
        "Banking::SafeDepositBox",
        "SafeDepositBox",
        "branch_code.value, box_number.value",
        &with_references,
        &[
            crate::kernel::GivenSpec { description: "customer is active", expr: Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: false }, left: Box::new(Expr::Lookup("customer.status")), right: Box::new(Expr::Str("active".to_string())) }, corrects_event: None },
        ],
        Some(crate::kernel::TransitionCheck { field: "status", from_states: &["vacant"] }),
        |record| {
        record.customer = Some(args.customer.clone());
        record.branch_code = Some(args.branch_code.clone());
        record.box_number = Some(args.box_number.clone());
        record.size = Some(args.size.clone());
        record.status = "rented".to_string();
            Ok(())
        },
        &[

        ],
        &safedepositbox_invariants(),
        &["BoxRented"],
        args.to_json(),
        mutations,
        seed_projections,
    )
}

impl RentArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![        ("branch_code".to_string(), self.branch_code.to_json()),
        ("box_number".to_string(), self.box_number.to_json()),
        ("size".to_string(), self.size.to_json()),
        ("customer".to_string(), crate::kernel::Json::Str(self.customer.clone())),]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}

impl RentArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("RentArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["branch_code", "box_number", "size", "customer", "id", "reference", "end_to_end"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Rent does not declare {} — it takes branch_code, box_number, size, customer",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["box_number", "branch_code", "customer", "size"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "Rent"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "branch_code, box_number, size, customer"),
    ])));
}
        let branch_code = BranchCode::from_json(&v.get("branch_code").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("RentArgs.branch_code expects BranchCode, got nil".to_string()))?.coerce_single_field("value"))?;
        branch_code.check_invariants()?;
        let box_number = BoxNumber::from_json(&v.get("box_number").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("RentArgs.box_number expects BoxNumber, got nil".to_string()))?.coerce_single_field("value"))?;
        box_number.check_invariants()?;
        let size = Size::from_json(&v.get("size").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("RentArgs.size expects Size, got nil".to_string()))?.coerce_single_field("value"))?;
        let customer = { let x = v.get("customer").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("RentArgs.customer expects String, got nil".to_string()))?; x.as_str().map(|s| s.to_string()).ok_or_else(|| if matches!(x, crate::kernel::Json::Array(_) | crate::kernel::Json::Object(_) | crate::kernel::Json::Null) { crate::kernel::Refusal::TypeMismatch(format!("RentArgs.customer expects String, got {}", x.inspect())) } else { crate::kernel::Refusal::TypeMismatch("RentArgs.customer: expected String".to_string()) })? };
        Ok(Self {
        branch_code,
        box_number,
        size,
        customer,
        })
    }
}

impl crate::kernel::Fielded for SurrenderArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {

            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct SurrenderArgs {
}

pub fn dispatch_surrender(
    repo: &mut impl crate::kernel::Repository<SafeDepositBox>, id: &str, args: SurrenderArgs, mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>,
) -> crate::kernel::DispatchResult<SafeDepositBox> {

    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, SAFE_DEPOSIT_BOX_PROJECTED_FIELDS);

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Act { id: id.to_string() },
        "Surrender",
        "Banking::SafeDepositBox",
        "SafeDepositBox",
        "branch_code.value, box_number.value",
        &with_references,
        &[
            crate::kernel::GivenSpec { description: "customer is active", expr: Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: false }, left: Box::new(Expr::Lookup("customer_status")), right: Box::new(Expr::Str("active".to_string())) }, corrects_event: None },
        ],
        Some(crate::kernel::TransitionCheck { field: "status", from_states: &["rented"] }),
        |record| {
        record.status = "vacant".to_string();
            Ok(())
        },
        &[

        ],
        &safedepositbox_invariants(),
        &["BoxSurrendered", "KeyReturnDue"],
        args.to_json(),
        mutations,
        seed_projections,
    )
}

impl SurrenderArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}

impl SurrenderArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("SurrenderArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["id", "safe_deposit_box", "branch_code", "box_number", "reference", "end_to_end"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Surrender does not declare {} — it takes ",
        unknown.join(", ")
    )));
}
        Ok(Self {

        })
    }
}

impl crate::kernel::Fielded for LogVisitArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "date" => Some(Field::Nested(&self.date)),
            "sequence" => Some(Field::Nested(&self.sequence)),
            "note" => self.note.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct LogVisitArgs {
    pub date: VisitDate,
    pub sequence: VisitSequence,
    pub note: Option<VisitNote>,
}

pub fn dispatch_log_visit(
    repo: &mut impl crate::kernel::Repository<SafeDepositBox>, id: &str, args: LogVisitArgs, mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>,
) -> crate::kernel::DispatchResult<SafeDepositBox> {
        args.date.check_invariants()?;
        args.sequence.check_invariants()?;
        if let Some(v) = &args.note { v.check_invariants()?; }
    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, SAFE_DEPOSIT_BOX_PROJECTED_FIELDS);

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Act { id: id.to_string() },
        "LogVisit",
        "Banking::SafeDepositBox",
        "SafeDepositBox",
        "branch_code.value, box_number.value",
        &with_references,
        &[
            crate::kernel::GivenSpec { description: "customer is active", expr: Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: false }, left: Box::new(Expr::Lookup("customer_status")), right: Box::new(Expr::Str("active".to_string())) }, corrects_event: None },
        ],
        Some(crate::kernel::TransitionCheck { field: "status", from_states: &["rented"] }),
        |record| {
        record.visits.push(Visit { date: args.date.clone(), sequence: args.sequence.clone(), note: args.note.clone(), state: "logged".to_string() });
            Ok(())
        },
        &[

        ],
        &safedepositbox_invariants(),
        &["BoxOpened"],
        args.to_json(),
        mutations,
        seed_projections,
    )
}

impl LogVisitArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![        ("date".to_string(), self.date.to_json()),
        ("sequence".to_string(), self.sequence.to_json()),
        ("note".to_string(), self.note.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}

impl LogVisitArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("LogVisitArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["date", "sequence", "note", "id", "safe_deposit_box", "branch_code", "box_number", "reference", "end_to_end"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "LogVisit does not declare {} — it takes date, sequence, note",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["date", "sequence"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "LogVisit"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "date, sequence, note"),
    ])));
}
        let date = VisitDate::from_json(&v.get("date").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("LogVisitArgs.date expects VisitDate, got nil".to_string()))?.coerce_single_field("value"))?;
        date.check_invariants()?;
        let sequence = VisitSequence::from_json(&v.get("sequence").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("LogVisitArgs.sequence expects VisitSequence, got nil".to_string()))?.coerce_single_field("value"))?;
        sequence.check_invariants()?;
        let note = match v.get("note") { Some(crate::kernel::Json::Null) | None => None, Some(x) => Some(VisitNote::from_json(&x.coerce_single_field("text"))?) };
        if let Some(v) = &note { v.check_invariants()?; }
        Ok(Self {
        date,
        sequence,
        note,
        })
    }
}

impl crate::kernel::Fielded for IssueKeyArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {
            "serial" => Some(Field::Nested(&self.serial)),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct IssueKeyArgs {
    pub serial: KeySerial,
}

pub fn dispatch_issue_key(
    repo: &mut impl crate::kernel::Repository<SafeDepositBox>, id: &str, args: IssueKeyArgs, mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>,
) -> crate::kernel::DispatchResult<SafeDepositBox> {
        args.serial.check_invariants()?;
    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, SAFE_DEPOSIT_BOX_PROJECTED_FIELDS);

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Act { id: id.to_string() },
        "IssueKey",
        "Banking::SafeDepositBox",
        "SafeDepositBox",
        "branch_code.value, box_number.value",
        &with_references,
        &[
            crate::kernel::GivenSpec { description: "customer is active", expr: Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: false }, left: Box::new(Expr::Lookup("customer_status")), right: Box::new(Expr::Str("active".to_string())) }, corrects_event: None },
        ],
        Some(crate::kernel::TransitionCheck { field: "status", from_states: &["rented"] }),
        |record| {
        record.keys.push(KeyIssuance { serial: args.serial.clone(), status: "issued".to_string() });
            Ok(())
        },
        &[

        ],
        &safedepositbox_invariants(),
        &["KeyIssued"],
        args.to_json(),
        mutations,
        seed_projections,
    )
}

impl IssueKeyArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![        ("serial".to_string(), self.serial.to_json()),]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}

impl IssueKeyArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("IssueKeyArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["serial", "id", "safe_deposit_box", "branch_code", "box_number", "reference", "end_to_end"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "IssueKey does not declare {} — it takes serial",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["serial"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "IssueKey"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "serial"),
    ])));
}
        let serial = KeySerial::from_json(&v.get("serial").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("IssueKeyArgs.serial expects KeySerial, got nil".to_string()))?.coerce_single_field("value"))?;
        serial.check_invariants()?;
        Ok(Self {
        serial,
        })
    }
}

