// GENERATED by bin/project_rust from spec/fixtures/rust_project/has_many_fixture's canonical IR.
// Do not hand-edit — re-run bin/project_rust instead.
#![allow(dead_code, unused_variables)]
use crate::kernel::Expr;
use crate::generated::has_many_fixture::member::Handle;

#[derive(Debug, Clone, PartialEq)]
pub struct CircleId {
    pub value: String,
}

impl crate::kernel::Fielded for CircleId {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "value" => Some(Field::Value(Value::Str(self.value.clone()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        match self.field("value") { Some(crate::kernel::Field::Value(v)) => Some(v), _ => None }
    }
}


impl CircleId {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {
        if !crate::kernel::pattern::matches("[^ \\t\\n\\r]", &self.value) { return Err(crate::kernel::Refusal::TypeMismatch(format!("{}{:?}", "CircleId.value must match [^ \\t\\n\\r], got ", self.value))); }
{
    let ctx = crate::kernel::EvalContext { args: &crate::kernel::NoFields, instance: self };
    if !crate::kernel::interpret(&Expr::Not(Box::new(Expr::Empty(Box::new(Expr::ToS(Box::new(Expr::Lookup("value"))))))), &ctx)?.truthy() {
        let mut offered = self.to_json();
        if let crate::kernel::Json::Object(fields) = &mut offered {
            fields.sort_by(|a, b| a.0.cmp(&b.0));
        }
        let offered = offered.to_json_string();
        return Err(crate::kernel::Refusal::InvariantViolation(crate::kernel::RefusalSite::InvariantViolationValueObjectInvariant.render(&[
            ("name", "CircleId"),
            ("description", "a circle is referenced"),
            ("offered", offered.as_str()),
        ])));
    }
}
        Ok(())
    }
}

impl CircleId {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("value".to_string(), crate::kernel::Json::Str(self.value.clone())),
        ])
    }
}

impl CircleId {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("CircleId expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["value"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "CircleId does not declare {} — it takes value",
        unknown.join(", ")
    )));
}
        Ok(Self {
        value: { let x = v.get("value").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("CircleId.value expects String, got nil".to_string()))?; x.as_str().map(|s| s.to_string()).ok_or_else(|| if matches!(x, crate::kernel::Json::Array(_) | crate::kernel::Json::Object(_) | crate::kernel::Json::Null) { crate::kernel::Refusal::TypeMismatch(format!("CircleId.value expects String, got {}", x.inspect())) } else { crate::kernel::Refusal::TypeMismatch("CircleId.value: expected String".to_string()) })? },
        })
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Circle {
    pub id: Option<CircleId>,
    pub members: Vec<String>,
}

impl crate::kernel::Fielded for Circle {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::{Field, Value};
        match name {
            "id" => self.id.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            "members" => Some(Field::Value(Value::List(self.members.len()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {
            "members" => Some(self.members.iter().map(|v| Field::Value(Value::Str(v.clone()))).collect()),
            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}

impl Circle {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("id".to_string(), self.id.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),
        ("members".to_string(), crate::kernel::Json::Array(self.members.iter().map(|x| crate::kernel::Json::Str(x.clone())).collect())),
        ])
    }
}

impl Circle {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("Circle expects an object, got {}", v.inspect())));
}
        Ok(Self {
        id: match v.get("id") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(CircleId::from_json(&x.coerce_single_field("value"))?), },
        members: match v.get("members").and_then(crate::kernel::Json::as_array) { Some(items) => items.iter().map(|item| item.as_str().map(|s| s.to_string()).ok_or_else(|| if matches!(item, crate::kernel::Json::Array(_) | crate::kernel::Json::Object(_) | crate::kernel::Json::Null) { crate::kernel::Refusal::TypeMismatch(format!("Circle.members expects String, got {}", item.inspect())) } else { crate::kernel::Refusal::TypeMismatch("Circle.members: expected String".to_string()) })).collect::<Result<Vec<_>, crate::kernel::Refusal>>()?, None => Vec::new(), },
        })
    }
}

impl crate::kernel::ToJson for Circle {
    fn to_json(&self) -> crate::kernel::Json {
        Circle::to_json(self)
    }
}

impl crate::kernel::SetProjectedField for Circle {
    fn set_projected_field(&mut self, name: &'static str, value: Option<String>) {
        match name {

            _ => {}
        }
    }
}

pub static CIRCLE_PROJECTED_FIELDS: &[crate::kernel::ProjectedFieldSpec] = &[

];

impl Circle {
    pub fn extract_id(v: &crate::kernel::Json) -> Result<String, crate::kernel::Refusal> {
        let by_identity = (|| -> Option<String> {
            let c0 = v.dig("id.value")?.to_id_component().ok()?;
            Some(c0)
        })();
        let by_id_key = v.get("id").and_then(|j| j.to_id_component().ok());
        let by_reference_key = v.get("circle").and_then(|j| j.to_id_component().ok());

        by_identity.or(by_id_key).or(by_reference_key).ok_or_else(|| {
            crate::kernel::Refusal::TypeMismatch("Circle: no identity found (tried id.value, id, circle)".to_string())
        })
    }
}

fn circle_invariants() -> crate::kernel::InvariantSet {
    use crate::kernel::Expr;
    crate::kernel::InvariantSet {
        aggregate: vec![],
        entities: vec![],
    }
}

impl crate::kernel::Fielded for OpenArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {
            "id" => Some(Field::Nested(&self.id)),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct OpenArgs {
    pub id: CircleId,
}

pub fn dispatch_open(
    repo: &mut impl crate::kernel::Repository<Circle>, args: OpenArgs, mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>,
) -> crate::kernel::DispatchResult<Circle> {
        args.id.check_invariants()?;
    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, CIRCLE_PROJECTED_FIELDS);

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Create {
        id: args.id.value.to_string(),
        build: Box::new(|| Circle {
            id: Some(args.id.clone()),
            members: vec![],
        }),
        state_independent: true,
    },
        "Open",
        "HasManyFixture::Circle",
        "Circle",
        "id.value",
        &with_references,
        &[

        ],
        None,
        |record| {
        record.id = Some(args.id.clone());
            Ok(())
        },
        &[

        ],
        &circle_invariants(),
        &["CircleOpened"],
        args.to_json(),
        mutations,
        seed_projections,
    )
}

impl OpenArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![        ("id".to_string(), self.id.to_json()),]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}

impl OpenArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("OpenArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["id"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Open does not declare {} — it takes id",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["id"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "Open"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "id"),
    ])));
}
        let id = CircleId::from_json(&(match v.get("id").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("OpenArgs.id expects CircleId, got nil".to_string()))? { crate::kernel::Json::Null => crate::kernel::Json::Object(Vec::new()), other => other.clone() }).coerce_single_field("value"))?;
        id.check_invariants()?;
        Ok(Self {
        id,
        })
    }
}

impl crate::kernel::Fielded for AdmitArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "members" => Some(Field::Value(Value::List(self.members.len()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {
            "members" => Some(self.members.iter().map(|v| Field::Nested(v)).collect()),
            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct AdmitArgs {
    pub members: Vec<Handle>,
}

pub fn dispatch_admit(
    repo: &mut impl crate::kernel::Repository<Circle>, id: &str, args: AdmitArgs, mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>,
) -> crate::kernel::DispatchResult<Circle> {
        for item in &args.members { item.check_invariants()?; }
    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, CIRCLE_PROJECTED_FIELDS);

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Act { id: id.to_string() },
        "Admit",
        "HasManyFixture::Circle",
        "Circle",
        "id.value",
        &with_references,
        &[

        ],
        None,
        |record| {
        record.members = args.members.iter().map(|item| item.value.clone()).collect();
            Ok(())
        },
        &[

        ],
        &circle_invariants(),
        &["MembersAdmitted"],
        args.to_json(),
        mutations,
        seed_projections,
    )
}

impl AdmitArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![        ("members".to_string(), crate::kernel::Json::Array(self.members.iter().map(|x| x.to_json()).collect())),]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}

impl AdmitArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("AdmitArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["members", "id", "circle"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Admit does not declare {} — it takes members",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["members"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "Admit"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "members"),
    ])));
}
        let members = match v.get("members").and_then(crate::kernel::Json::as_array) { Some(items) => items.iter().map(Handle::from_json).collect::<Result<Vec<_>, crate::kernel::Refusal>>()?, None => Vec::new(), };
        for item in &members { item.check_invariants()?; }
        Ok(Self {
        members,
        })
    }
}

