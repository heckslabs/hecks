// GENERATED by bin/project_rust from examples/embryonaut_vendoring_demo's canonical IR.
// Do not hand-edit — re-run bin/project_rust instead.
#![allow(dead_code, unused_variables)]
use crate::kernel::Expr;

#[derive(Debug, Clone, PartialEq)]
pub struct GadgetName {
    pub value: String,
}

impl crate::kernel::Fielded for GadgetName {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "value" => Some(Field::Value(Value::Str(self.value.clone()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        match self.field("value") { Some(crate::kernel::Field::Value(v)) => Some(v), _ => None }
    }
}


impl GadgetName {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {

        Ok(())
    }
}

impl GadgetName {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("value".to_string(), crate::kernel::Json::Str(self.value.clone())),
        ])
    }
}

impl GadgetName {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("GadgetName expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["value"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "GadgetName does not declare {} — it takes value",
        unknown.join(", ")
    )));
}
        Ok(Self {
        value: { let x = v.get("value").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("GadgetName.value expects String, got nil".to_string()))?; x.as_str().map(|s| s.to_string()).ok_or_else(|| if matches!(x, crate::kernel::Json::Array(_) | crate::kernel::Json::Object(_) | crate::kernel::Json::Null) { crate::kernel::Refusal::TypeMismatch(format!("GadgetName.value expects String, got {}", x.inspect())) } else { crate::kernel::Refusal::TypeMismatch("GadgetName.value: expected String".to_string()) })? },
        })
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum GadgetStatus {
    Idle,
    Active,
}

impl crate::kernel::Fielded for GadgetStatus {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::{Field, Value};
        match name {
            "value" => Some(Field::Value(Value::Str(match self { GadgetStatus::Idle => "idle".to_string(), GadgetStatus::Active => "active".to_string(), }))),
            _ => None,
        }
    }
    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        match self.field("value") { Some(crate::kernel::Field::Value(v)) => Some(v), _ => None }
    }
}

impl GadgetStatus {
    pub fn to_json(&self) -> crate::kernel::Json {
        let member = match self {
            GadgetStatus::Idle => "idle",
            GadgetStatus::Active => "active",
        };
        crate::kernel::Json::obj(vec![("value", crate::kernel::Json::str(member))])
    }

    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
        // A `one_of` closed set is admission-checked on the RAW offered
        // value, no shape check first — `Value::Admission#admit_member`
        // runs on whatever `Value::Coercion#fields_for` auto-wrapped into
        // the sole attribute's slot (a bare Array, a Bool, anything), never
        // on a value already known to be a String. `v.dig` gives the same
        // tolerant unwrap Ruby's own `fields_for` does: the wrapped
        // `{"value": ...}` shape's inner value if `v` is an
        // object, or `v` itself untouched if it isn't (matching
        // `fields_for`'s single-field auto-wrap of a bare scalar/array/
        // whatever). Only THEN is admission checked — a non-member value
        // refuses `InvariantViolation`, matching Ruby's own refusal kind,
        // never `TypeMismatch` for a shape a member set never declared.
        //
        // BUG#14 (qa/bluebook/quality_control.bluebook) — a MISSING field
        // is not "a shape a member set never declared" the way a present-
        // but-wrong value is; it is `Value::Coercion#check_required_fields`
        // (runtime/value/coercion.rb) firing, and that check runs BEFORE
        // `admit_member` in `validate!`'s own order. A caller-supplied
        // `null` for a REQUIRED command argument of this type is translated
        // by `required_composite_argument_expr` (json_codec.rb, BUG#4) into
        // an EMPTY object — "build the value object from no fields at all",
        // matching `Value::Coercion#nil_argument`'s own `build(value_object,
        // {}, aggregate)` exactly — so `v.dig` above finding nothing is
        // genuinely indistinguishable, at this point, from a Hash-shaped
        // caller argument that simply never named the sole field's own key
        // either way: BOTH are that field's OWN absence, not a member
        // mismatch.
        // Before this fix, that absence still fell through to the admission
        // match below, stringified as `Json::Null`'s own `ruby_to_s` (never
        // a real member), so a required-but-omitted closed-set argument
        // always misreported `InvariantViolation` where Ruby raises
        // `TypeMismatch` ("{type}.{field} expects {expected}, got nil" —
        // the same `numeric_field` wording `required_field_expr` already
        // gives every OTHER composite field's own missing-key case).
        let candidate = v.dig("value").cloned().unwrap_or(crate::kernel::Json::Null);
        if matches!(candidate, crate::kernel::Json::Null) {
            return Err(crate::kernel::Refusal::TypeMismatch("GadgetStatus.value expects String, got nil".to_string()));
        }
        match candidate.ruby_to_s().as_str() {
            "idle" => Ok(GadgetStatus::Idle),
            "active" => Ok(GadgetStatus::Active),
            _ => Err(crate::kernel::Refusal::InvariantViolation(crate::kernel::RefusalSite::InvariantViolationClosedSetMember.render(&[
                ("type", "GadgetStatus"),
                ("admitted", "\"idle\", \"active\""),
                ("offered", &candidate.inspect()),
            ]))),
        }
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Gadget {
    pub name: Option<GadgetName>,
    pub status: Option<GadgetStatus>,
}

impl crate::kernel::Fielded for Gadget {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::{Field, Value};
        match name {
            "name" => self.name.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            "status" => self.status.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}

impl Gadget {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("name".to_string(), self.name.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),
        ("status".to_string(), self.status.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),
        ])
    }
}

impl Gadget {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("Gadget expects an object, got {}", v.inspect())));
}
        Ok(Self {
        name: match v.get("name") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(GadgetName::from_json(&x.coerce_single_field("value"))?), },
        status: match v.get("status") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(GadgetStatus::from_json(&x.coerce_single_field("value"))?), },
        })
    }
}

impl crate::kernel::ToJson for Gadget {
    fn to_json(&self) -> crate::kernel::Json {
        Gadget::to_json(self)
    }
}

impl crate::kernel::SetProjectedField for Gadget {
    fn set_projected_field(&mut self, name: &'static str, value: Option<String>) {
        match name {

            _ => {}
        }
    }
}

pub static GADGET_PROJECTED_FIELDS: &[crate::kernel::ProjectedFieldSpec] = &[

];

impl Gadget {
    pub fn extract_id(v: &crate::kernel::Json) -> Result<String, crate::kernel::Refusal> {
        let by_identity = (|| -> Option<String> {
            let c0 = v.dig("name.value")?.to_id_component().ok()?;
            Some(c0)
        })();
        let by_id_key = v.get("id").and_then(|j| j.to_id_component().ok());
        let by_reference_key = v.get("gadget").and_then(|j| j.to_id_component().ok());

        by_identity.or(by_id_key).or(by_reference_key).ok_or_else(|| {
            crate::kernel::Refusal::TypeMismatch("Gadget: no identity found (tried name.value, id, gadget)".to_string())
        })
    }
}

fn gadget_invariants() -> crate::kernel::InvariantSet {
    use crate::kernel::Expr;
    crate::kernel::InvariantSet {
        aggregate: vec![],
        entities: vec![],
    }
}

impl crate::kernel::Fielded for RegisterArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {
            "name" => Some(Field::Nested(&self.name)),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct RegisterArgs {
    pub name: GadgetName,
}

pub fn dispatch_register(
    repo: &mut impl crate::kernel::Repository<Gadget>, route: Option<&crate::kernel::RoutingEnvelope>, args: RegisterArgs, mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>, tenant_boundary_check: Result<(), crate::kernel::Refusal>,
) -> crate::kernel::DispatchResult<Gadget> {
        args.name.check_invariants()?;
    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, GADGET_PROJECTED_FIELDS);

    crate::kernel::dispatch(
        repo,
        match route {
        Some(__route) => {
        __route.require_depth(0)?;
        let __hydrate_id: String = args.name.value.to_string();
        if __route.aggregate() != __hydrate_id.as_str() {
            return Err(crate::kernel::Refusal::TypeMismatch(format!("Register routes to {:?}, but its identity facts name {:?}", __route.aggregate(), __hydrate_id)));
        }
        crate::kernel::Hydrate::Create {
        id: __hydrate_id,
        build: Box::new(|| Gadget {
            name: Some(args.name.clone()),
            status: None,
        }),
        state_independent: true,
    }
    }
        None => { let __hydrate_id: String = args.name.value.to_string(); crate::kernel::Hydrate::Create {
        id: __hydrate_id,
        build: Box::new(|| Gadget {
            name: Some(args.name.clone()),
            status: None,
        }),
        state_independent: true,
    } }
    },
        "Register",
        "EmbryonautVendoringDemo::Gadget",
        "Gadget",
        "name.value",
        &with_references,
        &[

        ],
        None,
        |record| {
        record.name = Some(args.name.clone());
        record.status = Some(GadgetStatus::Idle);
            Ok(())
        },
        &[

        ],
        &gadget_invariants(),
        &["GadgetRegistered"],
        args.to_json(),
        mutations,
        seed_projections,
        tenant_boundary_check,
    )
}

impl RegisterArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![        ("name".to_string(), self.name.to_json()),]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}

impl RegisterArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("RegisterArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["name", "id"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Register does not declare {} — it takes name",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["name"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "Register"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "name"),
    ])));
}
        let name = GadgetName::from_json(&(match v.get("name").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("RegisterArgs.name expects GadgetName, got nil".to_string()))? { crate::kernel::Json::Null => crate::kernel::Json::Object(Vec::new()), other => other.clone() }).coerce_single_field("value"))?;
        name.check_invariants()?;
        Ok(Self {
        name,
        })
    }
}

impl crate::kernel::Fielded for ActivateArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {

            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct ActivateArgs {
}

pub fn dispatch_activate(
    repo: &mut impl crate::kernel::Repository<Gadget>, id: &str, args: ActivateArgs, mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>, tenant_boundary_check: Result<(), crate::kernel::Refusal>,
) -> crate::kernel::DispatchResult<Gadget> {

    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, GADGET_PROJECTED_FIELDS);

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Act { id: id.to_string() },
        "Activate",
        "EmbryonautVendoringDemo::Gadget",
        "Gadget",
        "name.value",
        &with_references,
        &[
            crate::kernel::GivenSpec { description: "only an idle gadget may activate", expr: Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: false }, left: Box::new(Expr::Lookup("status.value")), right: Box::new(Expr::Str("idle".to_string())) }, corrects_event: None },
        ],
        None,
        |record| {
        record.status = Some(GadgetStatus::Active);
            Ok(())
        },
        &[

        ],
        &gadget_invariants(),
        &["GadgetActivated"],
        args.to_json(),
        mutations,
        seed_projections,
        tenant_boundary_check,
    )
}

impl ActivateArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}

impl ActivateArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("ActivateArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["id", "gadget", "name"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Activate does not declare {} — it takes none",
        unknown.join(", ")
    )));
}
        Ok(Self {

        })
    }
}

