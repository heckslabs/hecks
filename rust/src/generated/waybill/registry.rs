// GENERATED by bin/project_rust — the JSON command router
// `kernel::cli` dispatches every step through. Do not hand-edit —
// re-run bin/project_rust instead.
#![allow(dead_code, unused_variables)]

// `Repository::save` (from_seed, below) is a TRAIT method —
// `InMemoryRepository`'s own inherent methods (entries(), used
// by instances()) need no import, but save() does.
use crate::kernel::Repository;

// `Clone` — a DRY RUN (`cli::run`'s `{"dry_run": …}` step, `cli::serve`'s
// `snapshot`/`restore`) dispatches against a throwaway copy of the whole
// store: givens, mutations, ensures all run for real, nothing is kept.
#[derive(Clone)]
pub struct Store {
    pub consignment: crate::kernel::InMemoryRepository<crate::generated::waybill::consignment::Consignment>,
    pub manifest: crate::kernel::InMemoryRepository<crate::generated::waybill::manifest::Manifest>,
}

impl Store {
    pub fn new() -> Self {
        Self {
            consignment: crate::kernel::InMemoryRepository::new(),
            manifest: crate::kernel::InMemoryRepository::new(),
        }
    }

    /// Every aggregate this domain declared, dumped as
    /// "Domain::Aggregate#id" -> its own to_json() — the exact key
    /// shape bin/rust_conformance's own comparable["instances"]
    /// builds from Ruby (Fuzzing::Replay.call's own instance key
    /// format, read directly).
    pub fn instances(&self) -> Vec<(String, crate::kernel::Json)> {
        let mut instances = Vec::new();
for (id, record) in self.consignment.entries() {
    instances.push((format!("{}{}", "Waybill::Consignment#", id), record.to_json()));
}
for (id, record) in self.manifest.entries() {
    instances.push((format!("{}{}", "Waybill::Manifest#", id), record.to_json()));
}
        instances
    }

    /// Seeds a fresh `Store` from a prior `instances()` dump —
    /// the exact "Domain::Aggregate#id" -> state shape, read
    /// back instead of written. Not `Result`-returning on a
    /// non-Object `seed`: an absent/malformed seed just yields
    /// an empty Store, the same starting point `Store::new()`
    /// already gives a caller with no prior state to seed from.
    pub fn from_seed(seed: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
        let mut store = Self::new();
        if let crate::kernel::Json::Object(fields) = seed {
            for (key, value) in fields {
if let Some(id) = key.strip_prefix("Waybill::Consignment#") {
    store.consignment.save(id, crate::generated::waybill::consignment::Consignment::from_json(value)?);
    continue;
}
if let Some(id) = key.strip_prefix("Waybill::Manifest#") {
    store.manifest.save(id, crate::generated::waybill::manifest::Manifest::from_json(value)?);
    continue;
}
            }
        }
        Ok(store)
    }
}

/// `AggregateScan` — kernel/repository.rs's own trait, given a REAL
/// per-aggregate body here: one `if` per aggregate this domain declared,
/// the same "Domain::Aggregate" prefix `instances()`'s own dump arms
/// already use, each returning that ONE aggregate's own (id, to_json())
/// listing straight off its repository's `entries()`. Falls through to
/// the trait's own default (`None`) for any prefix that matches none of
/// them — kernel/cli.rs turns that into a clean "unknown aggregate"
/// refusal, never a panic.
impl crate::kernel::AggregateScan for Store {
    fn scan(&self, aggregate: &str) -> Option<Vec<(String, crate::kernel::Json)>> {
if aggregate == "Waybill::Consignment" {
    return Some(self.consignment.entries().map(|(id, record)| (id.clone(), record.to_json())).collect());
}
if aggregate == "Waybill::Manifest" {
    return Some(self.manifest.entries().map(|(id, record)| (id.clone(), record.to_json())).collect());
}
        None
    }
}

pub fn dispatch_by_name(
    store: &mut Store,
    verb: &str,
    args_json: &crate::kernel::Json,
    caller_role: Option<&str>,
    caller_actor_id: Option<&str>,
    mutations: &mut Vec<crate::kernel::MutationRecord>,
) -> Result<Vec<crate::kernel::Event>, crate::kernel::Refusal> {
    match verb {
          "Waybill::Consignment.Request" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let args = crate::generated::waybill::consignment::RequestArgs::from_json(facts_json)?;
                      args.reference.check_invariants()?;
                      args.number.check_invariants()?;
                      args.item.check_invariants()?;
              crate::kernel::check_role(Some("Customer"), "Request", caller_role, caller_actor_id, &*store, QUERIES)?;
              let owner_deref = Vec::new();
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::waybill::consignment::dispatch_request(&mut store.consignment, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Waybill::Consignment.Ship" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("ShipArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["id", "consignment", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Ship does not declare {} — it takes ",
        unknown.join(", ")
    )));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::waybill::consignment::Consignment::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("Ship acts on an existing Consignment — pass reference.value:".to_string()))?, };
              let args = crate::generated::waybill::consignment::ShipArgs::from_json(facts_json)?;
              crate::kernel::check_role(Some("System"), "Ship", caller_role, caller_actor_id, &*store, QUERIES)?;
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Waybill::Consignment", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::waybill::consignment::dispatch_ship(&mut store.consignment, &id, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Waybill::Consignment.Cancel" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("CancelArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["id", "consignment", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Cancel does not declare {} — it takes ",
        unknown.join(", ")
    )));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::waybill::consignment::Consignment::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("Cancel acts on an existing Consignment — pass reference.value:".to_string()))?, };
              let args = crate::generated::waybill::consignment::CancelArgs::from_json(facts_json)?;
              crate::kernel::check_role(Some("System"), "Cancel", caller_role, caller_actor_id, &*store, QUERIES)?;
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Waybill::Consignment", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::waybill::consignment::dispatch_cancel(&mut store.consignment, &id, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Waybill::Manifest.Open" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let args = crate::generated::waybill::manifest::OpenArgs::from_json(facts_json)?;
                      args.reference.check_invariants()?;
              crate::kernel::check_role(Some("Loader"), "Open", caller_role, caller_actor_id, &*store, QUERIES)?;
              let owner_deref = Vec::new();
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::waybill::manifest::dispatch_open(&mut store.manifest, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Waybill::Manifest.AddSlot" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("AddSlotArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["number", "id", "manifest", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "AddSlot does not declare {} — it takes number",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["number"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "AddSlot"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "number"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::waybill::manifest::Manifest::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("AddSlot acts on an existing Manifest — pass reference.value:".to_string()))?, };
              let args = crate::generated::waybill::manifest::AddSlotArgs::from_json(facts_json)?;
                      args.number.check_invariants()?;
              crate::kernel::check_role(Some("Loader"), "AddSlot", caller_role, caller_actor_id, &*store, QUERIES)?;
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Waybill::Manifest", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::waybill::manifest::dispatch_add_slot(&mut store.manifest, &id, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Waybill::Manifest.Slot.Fill" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let (parent_id, element_id, element_wants) = match route { Some(route) => { route.require_depth(1)?; let element_id = route.entities()[0].clone(); (route.aggregate().to_string(), element_id.clone(), element_id) }, None => { let parent_id = crate::generated::waybill::manifest::Manifest::extract_id(facts_json)?; let element_id = crate::generated::waybill::manifest::Slot::extract_id(facts_json)?; let element_wants = crate::generated::waybill::manifest::Slot::extract_wants(facts_json); (parent_id, element_id, element_wants) }, };
              let args = crate::generated::waybill::manifest::SlotFillEntityArgs::from_json(facts_json)?;
                      args.item.check_invariants()?;
              crate::kernel::check_role(Some("Loader"), "Fill", caller_role, caller_actor_id, &*store, QUERIES)?;
              let owner_deref: Vec<(&'static str, crate::kernel::DerefNode)> = Vec::new();
              let mut command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              if let Some(parent_node) = crate::kernel::parent_deref(&*store, REFERENCE_TABLE, "Waybill::Manifest", &parent_id) { command_deref.push(("parent", parent_node)); }
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::waybill::manifest::dispatch_entity_slot_fill(&mut store.manifest, &parent_id, &element_id, &element_wants, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Waybill::Manifest.Slot.Clear" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let (parent_id, element_id, element_wants) = match route { Some(route) => { route.require_depth(1)?; let element_id = route.entities()[0].clone(); (route.aggregate().to_string(), element_id.clone(), element_id) }, None => { let parent_id = crate::generated::waybill::manifest::Manifest::extract_id(facts_json)?; let element_id = crate::generated::waybill::manifest::Slot::extract_id(facts_json)?; let element_wants = crate::generated::waybill::manifest::Slot::extract_wants(facts_json); (parent_id, element_id, element_wants) }, };
              let args = crate::generated::waybill::manifest::SlotClearEntityArgs::from_json(facts_json)?;
                      args.item.check_invariants()?;
              crate::kernel::check_role(Some("Loader"), "Clear", caller_role, caller_actor_id, &*store, QUERIES)?;
              let owner_deref: Vec<(&'static str, crate::kernel::DerefNode)> = Vec::new();
              let mut command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              if let Some(parent_node) = crate::kernel::parent_deref(&*store, REFERENCE_TABLE, "Waybill::Manifest", &parent_id) { command_deref.push(("parent", parent_node)); }
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::waybill::manifest::dispatch_entity_slot_clear(&mut store.manifest, &parent_id, &element_id, &element_wants, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
        other => Err(crate::kernel::Refusal::TypeMismatch(format!("unknown command {other:?}"))),
    }
}

/// Ruby's own event payload is `payload: args` — the WHOLE hash the
/// caller passed to `dispatch`, not filtered to the command's own
/// declared attributes (an identity-reference argument like
/// `number:` is not a declared `CreditArgs` field, and still shows
/// up on `AccountCredited`'s payload). `args.to_json()` inside each
/// generated `dispatch_*` only ever sees the NARROWED, typed args
/// struct, so it structurally can't reproduce that — this replaces
/// whatever payload the generated dispatch built with the raw,
/// unfiltered `args_json` this router itself received, after the
/// fact, once dispatch has already decided whether to accept it.
/// Load-bearing for process managers, not just payload fidelity: a
/// saga leg that forwards `reference: :reference` into `Account.
/// Debit` (which doesn't declare a `reference` attribute) needs
/// that field to survive onto `AccountDebited`'s own payload for
/// `correlates_by` to find it downstream.
fn stamp_payload(events: Vec<crate::kernel::Event>, args_json: &crate::kernel::Json) -> Vec<crate::kernel::Event> {
    events.into_iter().map(|e| crate::kernel::Event { payload: args_json.clone(), ..e }).collect()
}

pub static REFERENCE_TABLE: crate::kernel::ReferenceTable = &[
    ("Waybill::Consignment", &[]),
    ("Waybill::Manifest", &[]),
];

impl crate::kernel::ReferenceLookup for Store {
    fn find_fielded(&self, target: &str, id: &str) -> Option<Box<dyn crate::kernel::Fielded>> {
if target == "Waybill::Consignment" {
    return self.consignment.find(id).map(|r| Box::new(r) as Box<dyn crate::kernel::Fielded>);
}
if target == "Waybill::Manifest" {
    return self.manifest.find(id).map(|r| Box::new(r) as Box<dyn crate::kernel::Fielded>);
}
        None
    }
}

pub const POLICIES: &[crate::kernel::PolicyRule] = &[

];

pub const CROSS_DOMAIN_POLICIES: &[crate::kernel::CrossDomainPolicyRule] = &[

];



pub const PROCESS_MANAGERS: &[crate::kernel::ProcessManagerDef] = &[
    crate::kernel::ProcessManagerDef { name: "Packing", correlates_by: "reference.value", starts_on: "ConsignmentRequested", ends_on: "ConsignmentShipped", initial_state: "packing", handlers: &[crate::kernel::Handler { event_type: "ConsignmentRequested", from_state: "packing", to_state: "packing", dispatches: &[crate::kernel::DispatchSpec { command_name: "Manifest.Open", with: &[("reference", crate::kernel::WithValue::Ref("reference"))], compensates: None }] }, crate::kernel::Handler { event_type: "ManifestOpened", from_state: "packing", to_state: "packing", dispatches: &[crate::kernel::DispatchSpec { command_name: "Manifest.AddSlot", with: &[("reference", crate::kernel::WithValue::Ref("reference")), ("number", crate::kernel::WithValue::Ref("number"))], compensates: None }] }, crate::kernel::Handler { event_type: "SlotAdded", from_state: "packing", to_state: "filling", dispatches: &[crate::kernel::DispatchSpec { command_name: "Manifest::Slot.Fill", with: &[("number", crate::kernel::WithValue::Ref("number")), ("item", crate::kernel::WithValue::Ref("item"))], compensates: Some(&crate::kernel::DispatchSpec { command_name: "Manifest::Slot.Clear", with: &[("number", crate::kernel::WithValue::Ref("number")), ("item", crate::kernel::WithValue::Ref("item"))], compensates: None }) }] }, crate::kernel::Handler { event_type: "SlotFilled", from_state: "filling", to_state: "filled", dispatches: &[crate::kernel::DispatchSpec { command_name: "Consignment.Ship", with: &[("reference", crate::kernel::WithValue::Ref("reference"))], compensates: None }] }, crate::kernel::Handler { event_type: "refused", from_state: "filling", to_state: "cancelled", dispatches: &[crate::kernel::DispatchSpec { command_name: "Consignment.Cancel", with: &[("reference", crate::kernel::WithValue::Ref("reference"))], compensates: None }] }] },
];

pub fn reference_key_for_aggregate(qualified_name: &str) -> Option<&'static str> {
    match qualified_name {
        "Waybill::Consignment" => Some("consignment"),
        "Waybill::Manifest" => Some("manifest"),
        _ => None,
    }
}

pub fn command_creates(verb: &str) -> bool {
    match verb {
        "Waybill::Consignment.Request" => true,
        "Waybill::Consignment.Ship" => false,
        "Waybill::Consignment.Cancel" => false,
        "Waybill::Manifest.Open" => true,
        "Waybill::Manifest.AddSlot" => false,
        "Waybill::Manifest.Slot.Fill" => false,
        "Waybill::Manifest.Slot.Clear" => false,
        _ => false,
    }
}

pub fn identity_head_for_aggregate(qualified_name: &str) -> Option<&'static str> {
    match qualified_name {
        "Waybill::Consignment" => Some("reference"),
        "Waybill::Manifest" => Some("reference"),
        _ => None,
    }
}

pub fn entity_identity_head_for_path(qualified_path: &str) -> Option<&'static str> {
    match qualified_path {
        "Waybill::Manifest.Slot" => Some("number"),
        _ => None,
    }
}

pub fn command_attributes_for_verb(verb: &str) -> &'static [&'static str] {
    match verb {
        "Waybill::Consignment.Request" => &["reference", "number", "item"],
        "Waybill::Consignment.Ship" => &[],
        "Waybill::Consignment.Cancel" => &[],
        "Waybill::Manifest.Open" => &["reference"],
        "Waybill::Manifest.AddSlot" => &["number"],
        "Waybill::Manifest.Slot.Fill" => &["item"],
        "Waybill::Manifest.Slot.Clear" => &["item"],
        _ => &[],
    }
}

pub const QUERIES: &[crate::kernel::QueryDef] = &[
crate::kernel::QueryDef {
    verb: "Waybill::Consignment.Requested",
    aggregate: "Waybill::Consignment",
    conditions: &[
        crate::kernel::QueryCondition { field: "status", comparator: crate::kernel::query_comparators::QueryComparator::Eq, value: crate::kernel::QueryConditionValue::Literal("requested") },
    ],
    order_by: Some(crate::kernel::query_ordering::OrderBy { field: "reference", descending: false, nulls: crate::kernel::query_ordering::NullsMode::Native }),
    offset: None,
    limit: None,
    authorization: None,
},
];

/// C3.7 for a named query's own arguments — `query_arg_checks`
/// (rust/project/queries.rb) has the full story.
pub fn check_query_args(verb: &str, args: &crate::kernel::Json) -> Result<(), crate::kernel::Refusal> {
    match verb {

        _ => Ok(()),
    }
}

pub const READ_MODELS: &[crate::kernel::read_model::ReadModelDef] = &[

];
