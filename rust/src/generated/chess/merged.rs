// GENERATED by bin/project_rust — the JSON command router
// `kernel::cli` dispatches every step through. Do not hand-edit —
// re-run bin/project_rust instead.
#![allow(dead_code, unused_variables)]

// `Repository::save` (from_seed, below) is a TRAIT method —
// `InMemoryRepository`'s own inherent methods (entries(), used
// by instances()) need no import, but save() does.
use crate::kernel::Repository;

// `Clone` — a DRY RUN (`cli::run`'s `{"dry_run": …}` step, `cli::serve`'s
// `snapshot`/`restore`) dispatches against a throwaway copy of the whole
// store: givens, mutations, ensures all run for real, nothing is kept.
#[derive(Clone)]
pub struct Store {
    pub game: crate::kernel::InMemoryRepository<crate::generated::chess::game::Game>,
}

impl Store {
    pub fn new() -> Self {
        Self {
            game: crate::kernel::InMemoryRepository::new(),
        }
    }

    /// Every aggregate this domain declared, dumped as
    /// "Domain::Aggregate#id" -> its own to_json() — the exact key
    /// shape bin/rust_conformance's own comparable["instances"]
    /// builds from Ruby (Fuzzing::Replay.call's own instance key
    /// format, read directly).
    pub fn instances(&self) -> Vec<(String, crate::kernel::Json)> {
        let mut instances = Vec::new();
for (id, record) in self.game.entries() {
    instances.push((format!("{}{}", "Chess::Game#", id), record.to_json()));
}
        instances
    }

    /// Seeds a fresh `Store` from a prior `instances()` dump —
    /// the exact "Domain::Aggregate#id" -> state shape, read
    /// back instead of written. Not `Result`-returning on a
    /// non-Object `seed`: an absent/malformed seed just yields
    /// an empty Store, the same starting point `Store::new()`
    /// already gives a caller with no prior state to seed from.
    pub fn from_seed(seed: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
        let mut store = Self::new();
        if let crate::kernel::Json::Object(fields) = seed {
            for (key, value) in fields {
if let Some(id) = key.strip_prefix("Chess::Game#") {
    store.game.save(id, crate::generated::chess::game::Game::from_json(value)?);
    continue;
}
            }
        }
        Ok(store)
    }
}

/// `AggregateScan` — kernel/repository.rs's own trait, given a REAL
/// per-aggregate body here: one `if` per aggregate this domain declared,
/// the same "Domain::Aggregate" prefix `instances()`'s own dump arms
/// already use, each returning that ONE aggregate's own (id, to_json())
/// listing straight off its repository's `entries()`. Falls through to
/// the trait's own default (`None`) for any prefix that matches none of
/// them — kernel/cli.rs turns that into a clean "unknown aggregate"
/// refusal, never a panic.
impl crate::kernel::AggregateScan for Store {
    fn scan(&self, aggregate: &str) -> Option<Vec<(String, crate::kernel::Json)>> {
if aggregate == "Chess::Game" {
    return Some(self.game.entries().map(|(id, record)| (id.clone(), record.to_json())).collect());
}
        None
    }
}

pub fn dispatch_by_name(
    store: &mut Store,
    verb: &str,
    args_json: &crate::kernel::Json,
    caller_role: Option<&str>,
    caller_actor_id: Option<&str>,
    mutations: &mut Vec<crate::kernel::MutationRecord>,
) -> Result<Vec<crate::kernel::Event>, crate::kernel::Refusal> {
    match verb {
          "Chess::Game.Start" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let args = crate::generated::chess::game::StartArgs::from_json(facts_json)?;
                      args.label.check_invariants()?;
              let owner_deref = Vec::new();
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::chess::game::dispatch_start(&mut store.game, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Chess::Game.PlacePiece" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::chess::game::Game::extract_id(facts_json)?, };
              let args = crate::generated::chess::game::PlacePieceArgs::from_json(facts_json)?;
                      args.id.check_invariants()?;
                      args.square.check_invariants()?;
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Chess::Game", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::chess::game::dispatch_place_piece(&mut store.game, &id, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Chess::Game.MovePiece" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::chess::game::Game::extract_id(facts_json)?, };
              let args = crate::generated::chess::game::MovePieceArgs::from_json(facts_json)?;
                      args.id.check_invariants()?;
                      args.destination.check_invariants()?;
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Chess::Game", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::chess::game::dispatch_move_piece(&mut store.game, &id, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Chess::Game.CapturePiece" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::chess::game::Game::extract_id(facts_json)?, };
              let args = crate::generated::chess::game::CapturePieceArgs::from_json(facts_json)?;
                      args.id.check_invariants()?;
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Chess::Game", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::chess::game::dispatch_capture_piece(&mut store.game, &id, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Chess::Game.AdvanceTurn" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::chess::game::Game::extract_id(facts_json)?, };
              let args = crate::generated::chess::game::AdvanceTurnArgs::from_json(facts_json)?;
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Chess::Game", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::chess::game::dispatch_advance_turn(&mut store.game, &id, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Chess::Game.DeclareCheck" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::chess::game::Game::extract_id(facts_json)?, };
              let args = crate::generated::chess::game::DeclareCheckArgs::from_json(facts_json)?;
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Chess::Game", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::chess::game::dispatch_declare_check(&mut store.game, &id, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Chess::Game.DeclareCheckmate" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::chess::game::Game::extract_id(facts_json)?, };
              let args = crate::generated::chess::game::DeclareCheckmateArgs::from_json(facts_json)?;
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Chess::Game", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::chess::game::dispatch_declare_checkmate(&mut store.game, &id, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Chess::Game.DeclareStalemate" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::chess::game::Game::extract_id(facts_json)?, };
              let args = crate::generated::chess::game::DeclareStalemateArgs::from_json(facts_json)?;
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Chess::Game", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::chess::game::dispatch_declare_stalemate(&mut store.game, &id, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Chess::Game.Resign" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::chess::game::Game::extract_id(facts_json)?, };
              let args = crate::generated::chess::game::ResignArgs::from_json(facts_json)?;
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Chess::Game", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::chess::game::dispatch_resign(&mut store.game, &id, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Chess::Game.OfferDraw" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::chess::game::Game::extract_id(facts_json)?, };
              let args = crate::generated::chess::game::OfferDrawArgs::from_json(facts_json)?;
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Chess::Game", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::chess::game::dispatch_offer_draw(&mut store.game, &id, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Chess::Game.AcceptDraw" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::chess::game::Game::extract_id(facts_json)?, };
              let args = crate::generated::chess::game::AcceptDrawArgs::from_json(facts_json)?;
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Chess::Game", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::chess::game::dispatch_accept_draw(&mut store.game, &id, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Chess::Game.DeclineDraw" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::chess::game::Game::extract_id(facts_json)?, };
              let args = crate::generated::chess::game::DeclineDrawArgs::from_json(facts_json)?;
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Chess::Game", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::chess::game::dispatch_decline_draw(&mut store.game, &id, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Chess::Game.Piece.Move" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let (parent_id, element_id, element_wants) = match route { Some(route) => { route.require_depth(1)?; let element_id = route.entities()[0].clone(); (route.aggregate().to_string(), element_id.clone(), element_id) }, None => { let parent_id = crate::generated::chess::game::Game::extract_id(facts_json)?; let element_id = crate::generated::chess::game::Piece::extract_id(facts_json)?; let element_wants = crate::generated::chess::game::Piece::extract_wants(facts_json); (parent_id, element_id, element_wants) }, };
              let args = crate::generated::chess::game::PieceMoveEntityArgs::from_json(facts_json)?;
                      args.id.check_invariants()?;
                      args.destination.check_invariants()?;
              let owner_deref: Vec<(&'static str, crate::kernel::DerefNode)> = Vec::new();
              let mut command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              if let Some(parent_node) = crate::kernel::parent_deref(&*store, REFERENCE_TABLE, "Chess::Game", &parent_id) { command_deref.push(("parent", parent_node)); }
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::chess::game::dispatch_entity_piece_move(&mut store.game, &parent_id, &element_id, &element_wants, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Chess::Game.Piece.Capture" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              let (parent_id, element_id, element_wants) = match route { Some(route) => { route.require_depth(1)?; let element_id = route.entities()[0].clone(); (route.aggregate().to_string(), element_id.clone(), element_id) }, None => { let parent_id = crate::generated::chess::game::Game::extract_id(facts_json)?; let element_id = crate::generated::chess::game::Piece::extract_id(facts_json)?; let element_wants = crate::generated::chess::game::Piece::extract_wants(facts_json); (parent_id, element_id, element_wants) }, };
              let args = crate::generated::chess::game::PieceCaptureEntityArgs::from_json(facts_json)?;
                      args.id.check_invariants()?;
              let owner_deref: Vec<(&'static str, crate::kernel::DerefNode)> = Vec::new();
              let mut command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              if let Some(parent_node) = crate::kernel::parent_deref(&*store, REFERENCE_TABLE, "Chess::Game", &parent_id) { command_deref.push(("parent", parent_node)); }
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::chess::game::dispatch_entity_piece_capture(&mut store.game, &parent_id, &element_id, &element_wants, args, mutations, owner_deref, command_deref).map(|(_, events)| stamp_payload(events, &payload))
          }
        other => Err(crate::kernel::Refusal::TypeMismatch(format!("unknown command {other:?}"))),
    }
}

/// Ruby's own event payload is `payload: args` — the WHOLE hash the
/// caller passed to `dispatch`, not filtered to the command's own
/// declared attributes (an identity-reference argument like
/// `number:` is not a declared `CreditArgs` field, and still shows
/// up on `AccountCredited`'s payload). `args.to_json()` inside each
/// generated `dispatch_*` only ever sees the NARROWED, typed args
/// struct, so it structurally can't reproduce that — this replaces
/// whatever payload the generated dispatch built with the raw,
/// unfiltered `args_json` this router itself received, after the
/// fact, once dispatch has already decided whether to accept it.
/// Load-bearing for process managers, not just payload fidelity: a
/// saga leg that forwards `reference: :reference` into `Account.
/// Debit` (which doesn't declare a `reference` attribute) needs
/// that field to survive onto `AccountDebited`'s own payload for
/// `correlates_by` to find it downstream.
fn stamp_payload(events: Vec<crate::kernel::Event>, args_json: &crate::kernel::Json) -> Vec<crate::kernel::Event> {
    events.into_iter().map(|e| crate::kernel::Event { payload: args_json.clone(), ..e }).collect()
}

pub static REFERENCE_TABLE: crate::kernel::ReferenceTable = &[
    ("Chess::Game", &[]),
];

impl crate::kernel::ReferenceLookup for Store {
    fn find_fielded(&self, target: &str, id: &str) -> Option<Box<dyn crate::kernel::Fielded>> {
if target == "Chess::Game" {
    return self.game.find(id).map(|r| Box::new(r) as Box<dyn crate::kernel::Fielded>);
}
        None
    }
}

pub const POLICIES: &[crate::kernel::PolicyRule] = &[
    crate::kernel::PolicyRule { policy_name: "OnWhiteMovedAdvanceTurn", event_name: "PieceMoved", event_qualifier: Some("Game"), target_verb: "Chess::Game.AdvanceTurn", for_each: None, for_each_key: None, with_spec: &[("turn", "\"black\"")], where_expr: Some(where_on_white_moved_advance_turn) },
    crate::kernel::PolicyRule { policy_name: "OnBlackMovedAdvanceTurn", event_name: "PieceMoved", event_qualifier: Some("Game"), target_verb: "Chess::Game.AdvanceTurn", for_each: None, for_each_key: None, with_spec: &[("turn", "\"white\"")], where_expr: Some(where_on_black_moved_advance_turn) },
    crate::kernel::PolicyRule { policy_name: "OnCheckDeclaredAfterMove", event_name: "PieceMoved", event_qualifier: Some("Game"), target_verb: "Chess::Game.DeclareCheck", for_each: None, for_each_key: None, with_spec: &[("by", ":by")], where_expr: Some(where_on_check_declared_after_move) },
    crate::kernel::PolicyRule { policy_name: "OnCheckmateDeclaredAfterMove", event_name: "PieceMoved", event_qualifier: Some("Game"), target_verb: "Chess::Game.DeclareCheckmate", for_each: None, for_each_key: None, with_spec: &[("by", ":by")], where_expr: Some(where_on_checkmate_declared_after_move) },
    crate::kernel::PolicyRule { policy_name: "OnStalemateDeclaredAfterMove", event_name: "PieceMoved", event_qualifier: Some("Game"), target_verb: "Chess::Game.DeclareStalemate", for_each: None, for_each_key: None, with_spec: &[("by", ":by")], where_expr: Some(where_on_stalemate_declared_after_move) },
];

fn where_on_white_moved_advance_turn() -> crate::kernel::Expr {
    use crate::kernel::Expr;
    Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: false }, left: Box::new(Expr::Lookup("by.value")), right: Box::new(Expr::Str("white".to_string())) }
}

fn where_on_black_moved_advance_turn() -> crate::kernel::Expr {
    use crate::kernel::Expr;
    Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: false }, left: Box::new(Expr::Lookup("by.value")), right: Box::new(Expr::Str("black".to_string())) }
}

fn where_on_check_declared_after_move() -> crate::kernel::Expr {
    use crate::kernel::Expr;
    Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: false }, left: Box::new(Expr::Lookup("outcome.value")), right: Box::new(Expr::Str("check".to_string())) }
}

fn where_on_checkmate_declared_after_move() -> crate::kernel::Expr {
    use crate::kernel::Expr;
    Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: false }, left: Box::new(Expr::Lookup("outcome.value")), right: Box::new(Expr::Str("checkmate".to_string())) }
}

fn where_on_stalemate_declared_after_move() -> crate::kernel::Expr {
    use crate::kernel::Expr;
    Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: false }, left: Box::new(Expr::Lookup("outcome.value")), right: Box::new(Expr::Str("stalemate".to_string())) }
}

pub const CROSS_DOMAIN_POLICIES: &[crate::kernel::CrossDomainPolicyRule] = &[

];



pub const PROCESS_MANAGERS: &[crate::kernel::ProcessManagerDef] = &[

];

pub fn reference_key_for_aggregate(qualified_name: &str) -> Option<&'static str> {
    match qualified_name {
        "Chess::Game" => Some("game"),
        _ => None,
    }
}

pub fn command_creates(verb: &str) -> bool {
    match verb {
        "Chess::Game.Start" => true,
        "Chess::Game.PlacePiece" => false,
        "Chess::Game.MovePiece" => false,
        "Chess::Game.CapturePiece" => false,
        "Chess::Game.AdvanceTurn" => false,
        "Chess::Game.DeclareCheck" => false,
        "Chess::Game.DeclareCheckmate" => false,
        "Chess::Game.DeclareStalemate" => false,
        "Chess::Game.Resign" => false,
        "Chess::Game.OfferDraw" => false,
        "Chess::Game.AcceptDraw" => false,
        "Chess::Game.DeclineDraw" => false,
        "Chess::Game.Piece.Move" => false,
        "Chess::Game.Piece.Capture" => false,
        _ => false,
    }
}

pub fn identity_head_for_aggregate(qualified_name: &str) -> Option<&'static str> {
    match qualified_name {
        "Chess::Game" => Some("label"),
        _ => None,
    }
}

pub fn command_attributes_for_verb(verb: &str) -> &'static [&'static str] {
    match verb {
        "Chess::Game.Start" => &["label"],
        "Chess::Game.PlacePiece" => &["id", "kind", "color", "square"],
        "Chess::Game.MovePiece" => &["id", "destination", "by", "outcome"],
        "Chess::Game.CapturePiece" => &["id", "by"],
        "Chess::Game.AdvanceTurn" => &["turn"],
        "Chess::Game.DeclareCheck" => &["by"],
        "Chess::Game.DeclareCheckmate" => &["by"],
        "Chess::Game.DeclareStalemate" => &["by"],
        "Chess::Game.Resign" => &["by"],
        "Chess::Game.OfferDraw" => &["by"],
        "Chess::Game.AcceptDraw" => &["by"],
        "Chess::Game.DeclineDraw" => &["by"],
        "Chess::Game.Piece.Move" => &["id", "destination", "by", "outcome"],
        "Chess::Game.Piece.Capture" => &["id", "by"],
        _ => &[],
    }
}

pub const QUERIES: &[crate::kernel::QueryDef] = &[
crate::kernel::QueryDef {
    verb: "Chess::Game.InProgress",
    aggregate: "Chess::Game",
    conditions: &[
        crate::kernel::QueryCondition { field: "status", comparator: crate::kernel::query_comparators::QueryComparator::Eq, value: crate::kernel::QueryConditionValue::Literal("in_progress") },
    ],
    order_by: None,
    offset: None,
    limit: None,
    authorization: None,
},
];

/// C3.7 for a named query's own arguments — `query_arg_checks`
/// (rust/project/queries.rb) has the full story.
pub fn check_query_args(verb: &str, args: &crate::kernel::Json) -> Result<(), crate::kernel::Refusal> {
    match verb {

        _ => Ok(()),
    }
}

pub const READ_MODELS: &[crate::kernel::read_model::ReadModelDef] = &[

];
