// GENERATED by bin/project_rust from qa/stress_domains/lease_clock's canonical IR.
// Do not hand-edit — re-run bin/project_rust instead.
#![allow(dead_code, unused_variables)]
use crate::kernel::Expr;

#[derive(Debug, Clone, PartialEq)]
pub struct LeaseKey {
    pub value: String,
}

impl crate::kernel::Fielded for LeaseKey {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "value" => Some(Field::Value(Value::Str(self.value.clone()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        match self.field("value") { Some(crate::kernel::Field::Value(v)) => Some(v), _ => None }
    }
}


impl LeaseKey {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {
        if !crate::kernel::pattern::matches("[^ \\t\\n\\r]", &self.value) { return Err(crate::kernel::Refusal::TypeMismatch(format!("{}{:?}", "LeaseKey.value must match [^ \\t\\n\\r], got ", self.value))); }
{
    let ctx = crate::kernel::EvalContext { args: &crate::kernel::NoFields, instance: self };
    if !crate::kernel::interpret(&Expr::Not(Box::new(Expr::Empty(Box::new(Expr::ToS(Box::new(Expr::Lookup("value"))))))), &ctx)?.truthy() {
        let mut offered = self.to_json();
        if let crate::kernel::Json::Object(fields) = &mut offered {
            fields.sort_by(|a, b| a.0.cmp(&b.0));
        }
        let offered = offered.to_json_string();
        return Err(crate::kernel::Refusal::InvariantViolation(crate::kernel::RefusalSite::InvariantViolationValueObjectInvariant.render(&[
            ("name", "LeaseKey"),
            ("description", "a lease is keyed"),
            ("offered", offered.as_str()),
        ])));
    }
}
        Ok(())
    }
}

impl LeaseKey {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("value".to_string(), crate::kernel::Json::Str(self.value.clone())),
        ])
    }
}

impl LeaseKey {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("LeaseKey expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["value"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "LeaseKey does not declare {} — it takes value",
        unknown.join(", ")
    )));
}
        Ok(Self {
        value: { let x = v.get("value").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("LeaseKey.value expects String, got nil".to_string()))?; x.as_str().map(|s| s.to_string()).ok_or_else(|| if matches!(x, crate::kernel::Json::Array(_) | crate::kernel::Json::Object(_) | crate::kernel::Json::Null) { crate::kernel::Refusal::TypeMismatch(format!("LeaseKey.value expects String, got {}", x.inspect())) } else { crate::kernel::Refusal::TypeMismatch("LeaseKey.value: expected String".to_string()) })? },
        })
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct LeaseHolder {
    pub value: String,
}

impl crate::kernel::Fielded for LeaseHolder {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "value" => Some(Field::Value(Value::Str(self.value.clone()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        match self.field("value") { Some(crate::kernel::Field::Value(v)) => Some(v), _ => None }
    }
}


impl LeaseHolder {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {
        if !crate::kernel::pattern::matches("[^ \\t\\n\\r]", &self.value) { return Err(crate::kernel::Refusal::TypeMismatch(format!("{}{:?}", "LeaseHolder.value must match [^ \\t\\n\\r], got ", self.value))); }
{
    let ctx = crate::kernel::EvalContext { args: &crate::kernel::NoFields, instance: self };
    if !crate::kernel::interpret(&Expr::Not(Box::new(Expr::Empty(Box::new(Expr::ToS(Box::new(Expr::Lookup("value"))))))), &ctx)?.truthy() {
        let mut offered = self.to_json();
        if let crate::kernel::Json::Object(fields) = &mut offered {
            fields.sort_by(|a, b| a.0.cmp(&b.0));
        }
        let offered = offered.to_json_string();
        return Err(crate::kernel::Refusal::InvariantViolation(crate::kernel::RefusalSite::InvariantViolationValueObjectInvariant.render(&[
            ("name", "LeaseHolder"),
            ("description", "a holder is named"),
            ("offered", offered.as_str()),
        ])));
    }
}
        Ok(())
    }
}

impl LeaseHolder {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("value".to_string(), crate::kernel::Json::Str(self.value.clone())),
        ])
    }
}

impl LeaseHolder {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("LeaseHolder expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["value"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "LeaseHolder does not declare {} — it takes value",
        unknown.join(", ")
    )));
}
        Ok(Self {
        value: { let x = v.get("value").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("LeaseHolder.value expects String, got nil".to_string()))?; x.as_str().map(|s| s.to_string()).ok_or_else(|| if matches!(x, crate::kernel::Json::Array(_) | crate::kernel::Json::Object(_) | crate::kernel::Json::Null) { crate::kernel::Refusal::TypeMismatch(format!("LeaseHolder.value expects String, got {}", x.inspect())) } else { crate::kernel::Refusal::TypeMismatch("LeaseHolder.value: expected String".to_string()) })? },
        })
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct LeaseInstant {
    pub value: i64,
}

impl crate::kernel::Fielded for LeaseInstant {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "value" => Some(Field::Value(Value::Int(self.value))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        match self.field("value") { Some(crate::kernel::Field::Value(v)) => Some(v), _ => None }
    }
}


impl LeaseInstant {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {
{
    let ctx = crate::kernel::EvalContext { args: &crate::kernel::NoFields, instance: self };
    if !crate::kernel::interpret(&Expr::Not(Box::new(Expr::SignTest { op: crate::kernel::Comparison { less_than: true, equal: false, negated: false }, receiver: Box::new(Expr::Lookup("value")) })), &ctx)?.truthy() {
        let mut offered = self.to_json();
        if let crate::kernel::Json::Object(fields) = &mut offered {
            fields.sort_by(|a, b| a.0.cmp(&b.0));
        }
        let offered = offered.to_json_string();
        return Err(crate::kernel::Refusal::InvariantViolation(crate::kernel::RefusalSite::InvariantViolationValueObjectInvariant.render(&[
            ("name", "LeaseInstant"),
            ("description", "a clock reading is not before the epoch"),
            ("offered", offered.as_str()),
        ])));
    }
}
        Ok(())
    }
}

impl LeaseInstant {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("value".to_string(), crate::kernel::Json::int(self.value)),
        ])
    }
}

impl LeaseInstant {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("LeaseInstant expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["value"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "LeaseInstant does not declare {} — it takes value",
        unknown.join(", ")
    )));
}
        Ok(Self {
        value: match v.get("value") { Some(x) => x.as_i64().ok_or_else(|| crate::kernel::Refusal::TypeMismatch(format!("LeaseInstant.value expects Integer, got {}", x.inspect())))?, None => 0 },
        })
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Lease {
    pub key: Option<LeaseKey>,
    pub holder: Option<LeaseHolder>,
    pub expires_at: Option<LeaseInstant>,
    pub status: String,
}

impl crate::kernel::Fielded for Lease {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::{Field, Value};
        match name {
            "key" => self.key.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            "holder" => self.holder.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            "expires_at" => self.expires_at.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            "status" => Some(Field::Value(Value::Str(self.status.clone()))),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}

impl Lease {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("key".to_string(), self.key.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),
        ("holder".to_string(), self.holder.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),
        ("expires_at".to_string(), self.expires_at.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),
        ("status".to_string(), crate::kernel::Json::Str(self.status.clone())),
        ])
    }
}

impl Lease {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("Lease expects an object, got {}", v.inspect())));
}
        Ok(Self {
        key: match v.get("key") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(LeaseKey::from_json(&x.coerce_single_field("value"))?), },
        holder: match v.get("holder") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(LeaseHolder::from_json(&x.coerce_single_field("value"))?), },
        expires_at: match v.get("expires_at") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(LeaseInstant::from_json(&x.coerce_single_field("value"))?), },
        status: v.require("status", "Lease")?.as_str().ok_or_else(|| crate::kernel::Refusal::TypeMismatch("Lease.status: expected a string".to_string()))?.to_string(),
        })
    }
}

impl crate::kernel::ToJson for Lease {
    fn to_json(&self) -> crate::kernel::Json {
        Lease::to_json(self)
    }
}

impl crate::kernel::SetProjectedField for Lease {
    fn set_projected_field(&mut self, name: &'static str, value: Option<String>) {
        match name {

            _ => {}
        }
    }
}

pub static LEASE_PROJECTED_FIELDS: &[crate::kernel::ProjectedFieldSpec] = &[

];

impl Lease {
    pub fn extract_id(v: &crate::kernel::Json) -> Result<String, crate::kernel::Refusal> {
        let by_identity = (|| -> Option<String> {
            let c0 = v.dig("key.value")?.to_id_component().ok()?;
            Some(c0)
        })();
        let by_id_key = v.get("id").and_then(|j| j.to_id_component().ok());
        let by_reference_key = v.get("lease").and_then(|j| j.to_id_component().ok());

        by_identity.or(by_id_key).or(by_reference_key).ok_or_else(|| {
            crate::kernel::Refusal::TypeMismatch("Lease: no identity found (tried key.value, id, lease)".to_string())
        })
    }
}

fn lease_invariants() -> crate::kernel::InvariantSet {
    use crate::kernel::Expr;
    crate::kernel::InvariantSet {
        aggregate: vec![],
        entities: vec![],
    }
}

impl crate::kernel::Fielded for RegisterArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {
            "key" => Some(Field::Nested(&self.key)),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct RegisterArgs {
    pub key: LeaseKey,
}

pub fn dispatch_register(
    repo: &mut impl crate::kernel::Repository<Lease>, args: RegisterArgs, mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>,
) -> crate::kernel::DispatchResult<Lease> {
        args.key.check_invariants()?;
    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, LEASE_PROJECTED_FIELDS);

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Create {
        id: args.key.value.to_string(),
        build: Box::new(|| Lease {
            key: Some(args.key.clone()),
            holder: None,
            expires_at: Some(LeaseInstant { value: 0 }),
            status: "free".to_string(),
        }),
        state_independent: true,
    },
        "Register",
        "LeaseClock::Lease",
        "Lease",
        "key.value",
        &with_references,
        &[

        ],
        None,
        |record| {
        record.key = Some(args.key.clone());
            Ok(())
        },
        &[

        ],
        &lease_invariants(),
        &["LeaseRegistered"],
        args.to_json(),
        mutations,
        seed_projections,
    )
}

impl RegisterArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![        ("key".to_string(), self.key.to_json()),]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}

impl RegisterArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("RegisterArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["key", "id"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Register does not declare {} — it takes key",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["key"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "Register"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "key"),
    ])));
}
        let key = LeaseKey::from_json(&(match v.get("key").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("RegisterArgs.key expects LeaseKey, got nil".to_string()))? { crate::kernel::Json::Null => crate::kernel::Json::Object(Vec::new()), other => other.clone() }).coerce_single_field("value"))?;
        key.check_invariants()?;
        Ok(Self {
        key,
        })
    }
}

impl crate::kernel::Fielded for AcquireArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {
            "holder" => Some(Field::Nested(&self.holder)),
            "now" => Some(Field::Nested(&self.now)),
            "expiry" => Some(Field::Nested(&self.expiry)),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct AcquireArgs {
    pub holder: LeaseHolder,
    pub now: LeaseInstant,
    pub expiry: LeaseInstant,
}

pub fn dispatch_acquire(
    repo: &mut impl crate::kernel::Repository<Lease>, id: &str, args: AcquireArgs, mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>,
) -> crate::kernel::DispatchResult<Lease> {
        args.holder.check_invariants()?;
        args.now.check_invariants()?;
        args.expiry.check_invariants()?;
    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, LEASE_PROJECTED_FIELDS);

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Act { id: id.to_string() },
        "Acquire",
        "LeaseClock::Lease",
        "Lease",
        "key.value",
        &with_references,
        &[
            crate::kernel::GivenSpec { description: "a live lease is not taken from its own holder", expr: Expr::Or(Box::new(Expr::Compare { op: crate::kernel::Comparison { less_than: false, equal: true, negated: true }, left: Box::new(Expr::Lookup("status")), right: Box::new(Expr::Str("held".to_string())) }), Box::new(Expr::Compare { op: crate::kernel::Comparison { less_than: true, equal: true, negated: false }, left: Box::new(Expr::Lookup("expires_at.value")), right: Box::new(Expr::Lookup("now.value")) })), corrects_event: None },
            crate::kernel::GivenSpec { description: "a lease's own new expiry is set after the moment it is acquired", expr: Expr::Compare { op: crate::kernel::Comparison { less_than: true, equal: true, negated: true }, left: Box::new(Expr::Lookup("expiry.value")), right: Box::new(Expr::Lookup("now.value")) }, corrects_event: None },
        ],
        Some(crate::kernel::TransitionCheck { field: "status", from_states: &["free", "held"] }),
        |record| {
        record.holder = Some(args.holder.clone());
        record.expires_at = Some(args.expiry.clone());
        record.status = "held".to_string();
            Ok(())
        },
        &[

        ],
        &lease_invariants(),
        &["LeaseAcquired"],
        args.to_json(),
        mutations,
        seed_projections,
    )
}

impl AcquireArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![        ("holder".to_string(), self.holder.to_json()),
        ("now".to_string(), self.now.to_json()),
        ("expiry".to_string(), self.expiry.to_json()),]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}

impl AcquireArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("AcquireArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["holder", "now", "expiry", "id", "lease", "key"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Acquire does not declare {} — it takes holder, now, expiry",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["expiry", "holder", "now"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "Acquire"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "holder, now, expiry"),
    ])));
}
        let holder = LeaseHolder::from_json(&(match v.get("holder").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("AcquireArgs.holder expects LeaseHolder, got nil".to_string()))? { crate::kernel::Json::Null => crate::kernel::Json::Object(Vec::new()), other => other.clone() }).coerce_single_field("value"))?;
        holder.check_invariants()?;
        let now = LeaseInstant::from_json(&(match v.get("now").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("AcquireArgs.now expects LeaseInstant, got nil".to_string()))? { crate::kernel::Json::Null => crate::kernel::Json::Object(Vec::new()), other => other.clone() }).coerce_single_field("value"))?;
        now.check_invariants()?;
        let expiry = LeaseInstant::from_json(&(match v.get("expiry").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("AcquireArgs.expiry expects LeaseInstant, got nil".to_string()))? { crate::kernel::Json::Null => crate::kernel::Json::Object(Vec::new()), other => other.clone() }).coerce_single_field("value"))?;
        expiry.check_invariants()?;
        Ok(Self {
        holder,
        now,
        expiry,
        })
    }
}

impl crate::kernel::Fielded for RenewArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {
            "now" => Some(Field::Nested(&self.now)),
            "expiry" => Some(Field::Nested(&self.expiry)),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct RenewArgs {
    pub now: LeaseInstant,
    pub expiry: LeaseInstant,
}

pub fn dispatch_renew(
    repo: &mut impl crate::kernel::Repository<Lease>, id: &str, args: RenewArgs, mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>,
) -> crate::kernel::DispatchResult<Lease> {
        args.now.check_invariants()?;
        args.expiry.check_invariants()?;
    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, LEASE_PROJECTED_FIELDS);

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Act { id: id.to_string() },
        "Renew",
        "LeaseClock::Lease",
        "Lease",
        "key.value",
        &with_references,
        &[
            crate::kernel::GivenSpec { description: "only a still-live lease is renewed", expr: Expr::Compare { op: crate::kernel::Comparison { less_than: true, equal: true, negated: true }, left: Box::new(Expr::Lookup("expires_at.value")), right: Box::new(Expr::Lookup("now.value")) }, corrects_event: None },
            crate::kernel::GivenSpec { description: "a renewal always extends the expiry, never shortens it", expr: Expr::Compare { op: crate::kernel::Comparison { less_than: true, equal: true, negated: true }, left: Box::new(Expr::Lookup("expiry.value")), right: Box::new(Expr::Lookup("expires_at.value")) }, corrects_event: None },
        ],
        Some(crate::kernel::TransitionCheck { field: "status", from_states: &["held"] }),
        |record| {
        record.expires_at = Some(args.expiry.clone());
        record.status = "held".to_string();
            Ok(())
        },
        &[

        ],
        &lease_invariants(),
        &["LeaseRenewed"],
        args.to_json(),
        mutations,
        seed_projections,
    )
}

impl RenewArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![        ("now".to_string(), self.now.to_json()),
        ("expiry".to_string(), self.expiry.to_json()),]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}

impl RenewArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("RenewArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["now", "expiry", "id", "lease", "key"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Renew does not declare {} — it takes now, expiry",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["expiry", "now"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "Renew"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "now, expiry"),
    ])));
}
        let now = LeaseInstant::from_json(&(match v.get("now").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("RenewArgs.now expects LeaseInstant, got nil".to_string()))? { crate::kernel::Json::Null => crate::kernel::Json::Object(Vec::new()), other => other.clone() }).coerce_single_field("value"))?;
        now.check_invariants()?;
        let expiry = LeaseInstant::from_json(&(match v.get("expiry").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("RenewArgs.expiry expects LeaseInstant, got nil".to_string()))? { crate::kernel::Json::Null => crate::kernel::Json::Object(Vec::new()), other => other.clone() }).coerce_single_field("value"))?;
        expiry.check_invariants()?;
        Ok(Self {
        now,
        expiry,
        })
    }
}

impl crate::kernel::Fielded for ReleaseArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {

            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct ReleaseArgs {
}

pub fn dispatch_release(
    repo: &mut impl crate::kernel::Repository<Lease>, id: &str, args: ReleaseArgs, mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>,
) -> crate::kernel::DispatchResult<Lease> {

    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, LEASE_PROJECTED_FIELDS);

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Act { id: id.to_string() },
        "Release",
        "LeaseClock::Lease",
        "Lease",
        "key.value",
        &with_references,
        &[

        ],
        Some(crate::kernel::TransitionCheck { field: "status", from_states: &["held"] }),
        |record| {
        record.status = "free".to_string();
            Ok(())
        },
        &[

        ],
        &lease_invariants(),
        &["LeaseReleased"],
        args.to_json(),
        mutations,
        seed_projections,
    )
}

impl ReleaseArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}

impl ReleaseArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("ReleaseArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["id", "lease", "key"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Release does not declare {} — it takes ",
        unknown.join(", ")
    )));
}
        Ok(Self {

        })
    }
}

impl crate::kernel::Fielded for ReapArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {
            "now" => Some(Field::Nested(&self.now)),
            _ => None,
        }
    }

    fn items(&self, name: &str) -> Option<Vec<crate::kernel::Field<'_>>> {
        #[allow(unused_imports)]
        use crate::kernel::{Field, Value};
        match name {

            _ => None,
        }
    }

    fn as_scalar(&self) -> Option<crate::kernel::Value> {
        None
    }
}


#[derive(Debug, Clone)]
pub struct ReapArgs {
    pub now: LeaseInstant,
}

pub fn dispatch_reap(
    repo: &mut impl crate::kernel::Repository<Lease>, id: &str, args: ReapArgs, mutations: &mut Vec<crate::kernel::MutationRecord>, owner_deref: Vec<(&'static str, crate::kernel::DerefNode)>, command_deref: Vec<(&'static str, crate::kernel::DerefNode)>,
) -> crate::kernel::DispatchResult<Lease> {
        args.now.check_invariants()?;
    let with_references = crate::kernel::WithReferences { command_deref: &command_deref, args: &args, owner_deref: &owner_deref };
    let seed_projections = crate::kernel::seeded_projections(&with_references, LEASE_PROJECTED_FIELDS);

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Act { id: id.to_string() },
        "Reap",
        "LeaseClock::Lease",
        "Lease",
        "key.value",
        &with_references,
        &[
            crate::kernel::GivenSpec { description: "only a lease whose own recorded expiry the passed clock has reached is reaped", expr: Expr::Compare { op: crate::kernel::Comparison { less_than: true, equal: true, negated: false }, left: Box::new(Expr::Lookup("expires_at.value")), right: Box::new(Expr::Lookup("now.value")) }, corrects_event: None },
        ],
        Some(crate::kernel::TransitionCheck { field: "status", from_states: &["held"] }),
        |record| {
        record.status = "free".to_string();
            Ok(())
        },
        &[

        ],
        &lease_invariants(),
        &["LeaseReaped"],
        args.to_json(),
        mutations,
        seed_projections,
    )
}

impl ReapArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(
            vec![        ("now".to_string(), self.now.to_json()),]
                .into_iter()
                .filter(|(_, v)| !matches!(v, crate::kernel::Json::Null))
                .collect(),
        )
    }
}

impl ReapArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("ReapArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["now", "id", "lease", "key"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Reap does not declare {} — it takes now",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["now"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "Reap"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "now"),
    ])));
}
        let now = LeaseInstant::from_json(&(match v.get("now").ok_or_else(|| crate::kernel::Refusal::TypeMismatch("ReapArgs.now expects LeaseInstant, got nil".to_string()))? { crate::kernel::Json::Null => crate::kernel::Json::Object(Vec::new()), other => other.clone() }).coerce_single_field("value"))?;
        now.check_invariants()?;
        Ok(Self {
        now,
        })
    }
}

