// GENERATED by bin/project_rust from /Users/christopheryoung/Projects/embryonaut's canonical IR.
// Do not hand-edit — re-run bin/project_rust instead.
#![allow(dead_code, unused_variables)]
use crate::kernel::Expr;

#[derive(Debug, Clone, PartialEq)]
pub struct EngagementReference {
    pub value: String,
}

impl crate::kernel::Fielded for EngagementReference {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "value" => Some(Field::Value(Value::Str(self.value.clone()))),
            _ => None,
        }
    }
}


impl EngagementReference {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {
{
    let ctx = crate::kernel::EvalContext { args: &crate::kernel::NoFields, instance: self };
    if !crate::kernel::interpret(&Expr::Not(Box::new(Expr::Empty(Box::new(Expr::ToS(Box::new(Expr::Lookup("value"))))))), &ctx)?.truthy() {
        return Err(crate::kernel::Refusal::InvariantViolation("EngagementReference violates its invariant: an engagement is referenced".to_string()));
    }
}
        Ok(())
    }
}

impl EngagementReference {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("value".to_string(), crate::kernel::Json::Str(self.value.clone())),
        ])
    }
}

impl EngagementReference {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
        Ok(Self {
        value: { let x = v.require("value", "EngagementReference")?; x.as_str().map(|s| s.to_string()).ok_or_else(|| crate::kernel::Refusal::TypeMismatch("EngagementReference.value: expected String".to_string()))? },
        })
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct UseCase {
    pub text: String,
}

impl crate::kernel::Fielded for UseCase {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "text" => Some(Field::Value(Value::Str(self.text.clone()))),
            _ => None,
        }
    }
}


impl UseCase {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {
{
    let ctx = crate::kernel::EvalContext { args: &crate::kernel::NoFields, instance: self };
    if !crate::kernel::interpret(&Expr::Not(Box::new(Expr::Empty(Box::new(Expr::ToS(Box::new(Expr::Lookup("text"))))))), &ctx)?.truthy() {
        return Err(crate::kernel::Refusal::InvariantViolation("UseCase violates its invariant: a use case says something".to_string()));
    }
}
        Ok(())
    }
}

impl UseCase {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("text".to_string(), crate::kernel::Json::Str(self.text.clone())),
        ])
    }
}

impl UseCase {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
        Ok(Self {
        text: { let x = v.require("text", "UseCase")?; x.as_str().map(|s| s.to_string()).ok_or_else(|| crate::kernel::Refusal::TypeMismatch("UseCase.text: expected String".to_string()))? },
        })
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct DomainReference {
    pub text: String,
}

impl crate::kernel::Fielded for DomainReference {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "text" => Some(Field::Value(Value::Str(self.text.clone()))),
            _ => None,
        }
    }
}


impl DomainReference {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {
{
    let ctx = crate::kernel::EvalContext { args: &crate::kernel::NoFields, instance: self };
    if !crate::kernel::interpret(&Expr::Not(Box::new(Expr::Empty(Box::new(Expr::ToS(Box::new(Expr::Lookup("text"))))))), &ctx)?.truthy() {
        return Err(crate::kernel::Refusal::InvariantViolation("DomainReference violates its invariant: a domain reference points somewhere".to_string()));
    }
}
        Ok(())
    }
}

impl DomainReference {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("text".to_string(), crate::kernel::Json::Str(self.text.clone())),
        ])
    }
}

impl DomainReference {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
        Ok(Self {
        text: { let x = v.require("text", "DomainReference")?; x.as_str().map(|s| s.to_string()).ok_or_else(|| crate::kernel::Refusal::TypeMismatch("DomainReference.text: expected String".to_string()))? },
        })
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct DemoReference {
    pub text: String,
}

impl crate::kernel::Fielded for DemoReference {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "text" => Some(Field::Value(Value::Str(self.text.clone()))),
            _ => None,
        }
    }
}


impl DemoReference {
    pub fn check_invariants(&self) -> Result<(), crate::kernel::Refusal> {
{
    let ctx = crate::kernel::EvalContext { args: &crate::kernel::NoFields, instance: self };
    if !crate::kernel::interpret(&Expr::Not(Box::new(Expr::Empty(Box::new(Expr::ToS(Box::new(Expr::Lookup("text"))))))), &ctx)?.truthy() {
        return Err(crate::kernel::Refusal::InvariantViolation("DemoReference violates its invariant: a demo reference points somewhere".to_string()));
    }
}
        Ok(())
    }
}

impl DemoReference {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("text".to_string(), crate::kernel::Json::Str(self.text.clone())),
        ])
    }
}

impl DemoReference {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
        Ok(Self {
        text: { let x = v.require("text", "DemoReference")?; x.as_str().map(|s| s.to_string()).ok_or_else(|| crate::kernel::Refusal::TypeMismatch("DemoReference.text: expected String".to_string()))? },
        })
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct Engagement {
    pub client_id: Option<String>,
    pub reference: Option<EngagementReference>,
    pub use_cases: Vec<UseCase>,
    pub domain_reference: Option<DomainReference>,
    pub demo_reference: Option<DemoReference>,
    pub stage: String,
}

impl crate::kernel::Fielded for Engagement {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::{Field, Value};
        match name {
            "client_id" => self.client_id.as_ref().map(|v| Field::Value(Value::Str(v.clone()))).or(Some(Field::Value(Value::Nil))),
            "reference" => self.reference.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            "use_cases" => Some(Field::Value(Value::List(self.use_cases.len()))),
            "domain_reference" => self.domain_reference.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            "demo_reference" => self.demo_reference.as_ref().map(|v| Field::Nested(v)).or(Some(Field::Value(Value::Nil))),
            "stage" => Some(Field::Value(Value::Str(self.stage.clone()))),
            _ => None,
        }
    }
}

impl Engagement {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("client_id".to_string(), self.client_id.as_ref().map(|v| crate::kernel::Json::Str(v.clone())).unwrap_or(crate::kernel::Json::Null)),
        ("reference".to_string(), self.reference.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),
        ("use_cases".to_string(), crate::kernel::Json::Array(self.use_cases.iter().map(|x| x.to_json()).collect())),
        ("domain_reference".to_string(), self.domain_reference.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),
        ("demo_reference".to_string(), self.demo_reference.as_ref().map(|v| v.to_json()).unwrap_or(crate::kernel::Json::Null)),
        ("stage".to_string(), crate::kernel::Json::Str(self.stage.clone())),
        ])
    }
}

impl Engagement {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
        Ok(Self {
        client_id: match v.get("client_id") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(x.as_str().map(|s| s.to_string()).ok_or_else(|| crate::kernel::Refusal::TypeMismatch("Engagement.client_id: expected String".to_string()))?), },
        reference: match v.get("reference") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(EngagementReference::from_json(x)?), },
        use_cases: match v.get("use_cases").and_then(crate::kernel::Json::as_array) { Some(items) => items.iter().map(UseCase::from_json).collect::<Result<Vec<_>, crate::kernel::Refusal>>()?, None => Vec::new(), },
        domain_reference: match v.get("domain_reference") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(DomainReference::from_json(x)?), },
        demo_reference: match v.get("demo_reference") { Some(&crate::kernel::Json::Null) | None => None, Some(x) => Some(DemoReference::from_json(x)?), },
        stage: v.require("stage", "Engagement")?.as_str().ok_or_else(|| crate::kernel::Refusal::TypeMismatch("Engagement.stage: expected a string".to_string()))?.to_string(),
        })
    }
}

impl crate::kernel::ToJson for Engagement {
    fn to_json(&self) -> crate::kernel::Json {
        Engagement::to_json(self)
    }
}
impl crate::kernel::SetProjectedField for Engagement {
    fn set_projected_field(&mut self, name: &'static str, value: Option<String>) {
        let _ = (name, value);
    }
}

impl Engagement {
    pub fn extract_id(v: &crate::kernel::Json) -> Result<String, crate::kernel::Refusal> {
        let by_identity = (|| -> Option<String> {
            let c0 = v.dig("reference.value")?.to_id_component().ok()?;
            Some(c0)
        })();
        let by_id_key = v.get("id").and_then(|j| j.to_id_component().ok());
        let by_reference_key = v.get("engagement").and_then(|j| j.to_id_component().ok());

        by_identity.or(by_id_key).or(by_reference_key).ok_or_else(|| {
            crate::kernel::Refusal::TypeMismatch("Engagement: no identity found (tried reference.value, id, engagement)".to_string())
        })
    }
}

impl crate::kernel::Fielded for ScheduleArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "client_id" => Some(Field::Value(Value::Str(self.client_id.clone()))),
            "reference" => Some(Field::Nested(&self.reference)),
            _ => None,
        }
    }
}


#[derive(Debug, Clone)]
pub struct ScheduleArgs {
    pub client_id: String,
    pub reference: EngagementReference,
}

pub fn dispatch_schedule(
    repo: &mut impl crate::kernel::Repository<Engagement>, args: ScheduleArgs, mutations: &mut Vec<crate::kernel::MutationRecord>,
) -> crate::kernel::DispatchResult<Engagement> {
        args.reference.check_invariants()?;

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Create {
        id: args.reference.value.to_string(),
        build: Box::new(|| Engagement {
            client_id: Some(args.client_id.clone()),
            reference: Some(args.reference.clone()),
            use_cases: vec![],
            domain_reference: None,
            demo_reference: None,
            stage: "workshop_scheduled".to_string(),
        }),
    },
        "Schedule",
        "Embryonaut::Engagement",
        "Engagement",
        "reference.value",
        &args,
        &[

        ],
        None,
        |record| {
        record.reference = Some(args.reference.clone());
            Ok(())
        },
        &[

        ],
        &engagement_invariants(),
        &["EngagementScheduled"],
        args.to_json(),
        mutations,
        Vec::new(),
    )
}

impl ScheduleArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("client_id".to_string(), crate::kernel::Json::Str(self.client_id.clone())),
        ("reference".to_string(), self.reference.to_json()),
        ])
    }
}

impl ScheduleArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
let unknown = v.unknown_keys(&["client_id", "reference", "id"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Schedule does not declare {} — it takes client_id, reference",
        unknown.join(", ")
    )));
}
        Ok(Self {
        client_id: { let x = v.require("client_id", "ScheduleArgs")?; x.as_str().map(|s| s.to_string()).ok_or_else(|| crate::kernel::Refusal::TypeMismatch("ScheduleArgs.client_id: expected String".to_string()))? },
        reference: EngagementReference::from_json(v.require("reference", "ScheduleArgs")?)?,
        })
    }
}

impl crate::kernel::Fielded for CompleteWorkshopArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        use crate::kernel::Value;
        match name {
            "use_cases" => Some(Field::Value(Value::List(self.use_cases.len()))),
            _ => None,
        }
    }
}


#[derive(Debug, Clone)]
pub struct CompleteWorkshopArgs {
    pub use_cases: Vec<UseCase>,
}

pub fn dispatch_complete_workshop(
    repo: &mut impl crate::kernel::Repository<Engagement>, id: &str, args: CompleteWorkshopArgs, mutations: &mut Vec<crate::kernel::MutationRecord>,
) -> crate::kernel::DispatchResult<Engagement> {
        for item in &args.use_cases { item.check_invariants()?; }

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Act { id: id.to_string() },
        "CompleteWorkshop",
        "Embryonaut::Engagement",
        "Engagement",
        "reference.value",
        &args,
        &[

        ],
        Some(crate::kernel::TransitionCheck { field: "stage", from_states: &["workshop_scheduled"] }),
        |record| {
        record.use_cases = args.use_cases.clone();
        record.stage = "workshop_completed".to_string();
            Ok(())
        },
        &[

        ],
        &engagement_invariants(),
        &["WorkshopCompleted"],
        args.to_json(),
        mutations,
        Vec::new(),
    )
}

impl CompleteWorkshopArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("use_cases".to_string(), crate::kernel::Json::Array(self.use_cases.iter().map(|x| x.to_json()).collect())),
        ])
    }
}

impl CompleteWorkshopArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
let unknown = v.unknown_keys(&["use_cases", "id", "engagement", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "CompleteWorkshop does not declare {} — it takes use_cases",
        unknown.join(", ")
    )));
}
        Ok(Self {
        use_cases: match v.get("use_cases").and_then(crate::kernel::Json::as_array) { Some(items) => items.iter().map(UseCase::from_json).collect::<Result<Vec<_>, crate::kernel::Refusal>>()?, None => Vec::new(), },
        })
    }
}

impl crate::kernel::Fielded for DraftDomainArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {
            "domain_reference" => Some(Field::Nested(&self.domain_reference)),
            _ => None,
        }
    }
}


#[derive(Debug, Clone)]
pub struct DraftDomainArgs {
    pub domain_reference: DomainReference,
}

pub fn dispatch_draft_domain(
    repo: &mut impl crate::kernel::Repository<Engagement>, id: &str, args: DraftDomainArgs, mutations: &mut Vec<crate::kernel::MutationRecord>,
) -> crate::kernel::DispatchResult<Engagement> {
        args.domain_reference.check_invariants()?;

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Act { id: id.to_string() },
        "DraftDomain",
        "Embryonaut::Engagement",
        "Engagement",
        "reference.value",
        &args,
        &[

        ],
        Some(crate::kernel::TransitionCheck { field: "stage", from_states: &["workshop_completed"] }),
        |record| {
        record.domain_reference = Some(args.domain_reference.clone());
        record.stage = "domain_drafted".to_string();
            Ok(())
        },
        &[

        ],
        &engagement_invariants(),
        &["DomainDrafted"],
        args.to_json(),
        mutations,
        Vec::new(),
    )
}

impl DraftDomainArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("domain_reference".to_string(), self.domain_reference.to_json()),
        ])
    }
}

impl DraftDomainArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
let unknown = v.unknown_keys(&["domain_reference", "id", "engagement", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "DraftDomain does not declare {} — it takes domain_reference",
        unknown.join(", ")
    )));
}
        Ok(Self {
        domain_reference: DomainReference::from_json(v.require("domain_reference", "DraftDomainArgs")?)?,
        })
    }
}

impl crate::kernel::Fielded for ShowDemoArgs {
    fn field(&self, name: &str) -> Option<crate::kernel::Field<'_>> {
        use crate::kernel::Field;
        
        match name {
            "demo_reference" => Some(Field::Nested(&self.demo_reference)),
            _ => None,
        }
    }
}


#[derive(Debug, Clone)]
pub struct ShowDemoArgs {
    pub demo_reference: DemoReference,
}

pub fn dispatch_show_demo(
    repo: &mut impl crate::kernel::Repository<Engagement>, id: &str, args: ShowDemoArgs, mutations: &mut Vec<crate::kernel::MutationRecord>,
) -> crate::kernel::DispatchResult<Engagement> {
        args.demo_reference.check_invariants()?;

    crate::kernel::dispatch(
        repo,
        crate::kernel::Hydrate::Act { id: id.to_string() },
        "ShowDemo",
        "Embryonaut::Engagement",
        "Engagement",
        "reference.value",
        &args,
        &[

        ],
        Some(crate::kernel::TransitionCheck { field: "stage", from_states: &["domain_drafted"] }),
        |record| {
        record.demo_reference = Some(args.demo_reference.clone());
        record.stage = "demo_shown".to_string();
            Ok(())
        },
        &[

        ],
        &engagement_invariants(),
        &["DemoShown"],
        args.to_json(),
        mutations,
        Vec::new(),
    )
}

impl ShowDemoArgs {
    pub fn to_json(&self) -> crate::kernel::Json {
        crate::kernel::Json::Object(vec![
        ("demo_reference".to_string(), self.demo_reference.to_json()),
        ])
    }
}

impl ShowDemoArgs {
    pub fn from_json(v: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
let unknown = v.unknown_keys(&["demo_reference", "id", "engagement", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "ShowDemo does not declare {} — it takes demo_reference",
        unknown.join(", ")
    )));
}
        Ok(Self {
        demo_reference: DemoReference::from_json(v.require("demo_reference", "ShowDemoArgs")?)?,
        })
    }
}


fn engagement_invariants() -> crate::kernel::InvariantSet {
    crate::kernel::InvariantSet { aggregate: vec![], entities: vec![] }
}
