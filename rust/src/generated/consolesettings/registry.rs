// GENERATED by bin/project_rust — the JSON command router
// `kernel::cli` dispatches every step through. Do not hand-edit —
// re-run bin/project_rust instead.
#![allow(dead_code, unused_variables)]

// `Repository::save` (from_seed, below) is a TRAIT method —
// `InMemoryRepository`'s own inherent methods (entries(), used
// by instances()) need no import, but save() does.
use crate::kernel::Repository;

// `Clone` — a DRY RUN (`cli::run`'s `{"dry_run": …}` step, `cli::serve`'s
// `snapshot`/`restore`) dispatches against a throwaway copy of the whole
// store: givens, mutations, ensures all run for real, nothing is kept.
#[derive(Clone)]
pub struct Store {
    pub statestyle: crate::kernel::InMemoryRepository<crate::generated::consolesettings::statestyle::StateStyle>,
    pub collection: crate::kernel::InMemoryRepository<crate::generated::consolesettings::collection::Collection>,
    pub overview: crate::kernel::InMemoryRepository<crate::generated::consolesettings::overview::Overview>,
}

impl Store {
    pub fn new() -> Self {
        Self {
            statestyle: crate::kernel::InMemoryRepository::new(),
            collection: crate::kernel::InMemoryRepository::new(),
            overview: crate::kernel::InMemoryRepository::new(),
        }
    }

    /// Every aggregate this domain declared, dumped as
    /// "Domain::Aggregate#id" -> its own to_json() — the exact key
    /// shape bin/rust_conformance's own comparable["instances"]
    /// builds from Ruby (Fuzzing::Replay.call's own instance key
    /// format, read directly).
    pub fn instances(&self) -> Vec<(String, crate::kernel::Json)> {
        let mut instances = Vec::new();
for (id, record) in self.statestyle.entries() {
    instances.push((format!("{}{}", "ConsoleSettings::StateStyle#", id), record.to_json()));
}
for (id, record) in self.collection.entries() {
    instances.push((format!("{}{}", "ConsoleSettings::Collection#", id), record.to_json()));
}
for (id, record) in self.overview.entries() {
    instances.push((format!("{}{}", "ConsoleSettings::Overview#", id), record.to_json()));
}
        instances
    }

    /// Seeds a fresh `Store` from a prior `instances()` dump —
    /// the exact "Domain::Aggregate#id" -> state shape, read
    /// back instead of written. Not `Result`-returning on a
    /// non-Object `seed`: an absent/malformed seed just yields
    /// an empty Store, the same starting point `Store::new()`
    /// already gives a caller with no prior state to seed from.
    pub fn from_seed(seed: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
        let mut store = Self::new();
        if let crate::kernel::Json::Object(fields) = seed {
            for (key, value) in fields {
if let Some(id) = key.strip_prefix("ConsoleSettings::StateStyle#") {
    store.statestyle.save(id, crate::generated::consolesettings::statestyle::StateStyle::from_json(value)?);
    continue;
}
if let Some(id) = key.strip_prefix("ConsoleSettings::Collection#") {
    store.collection.save(id, crate::generated::consolesettings::collection::Collection::from_json(value)?);
    continue;
}
if let Some(id) = key.strip_prefix("ConsoleSettings::Overview#") {
    store.overview.save(id, crate::generated::consolesettings::overview::Overview::from_json(value)?);
    continue;
}
            }
        }
        Ok(store)
    }
}

/// `AggregateScan` — kernel/repository.rs's own trait, given a REAL
/// per-aggregate body here: one `if` per aggregate this domain declared,
/// the same "Domain::Aggregate" prefix `instances()`'s own dump arms
/// already use, each returning that ONE aggregate's own (id, to_json())
/// listing straight off its repository's `entries()`. Falls through to
/// the trait's own default (`None`) for any prefix that matches none of
/// them — kernel/cli.rs turns that into a clean "unknown aggregate"
/// refusal, never a panic.
impl crate::kernel::AggregateScan for Store {
    fn scan(&self, aggregate: &str) -> Option<Vec<(String, crate::kernel::Json)>> {
if aggregate == "ConsoleSettings::StateStyle" {
    return Some(self.statestyle.entries().map(|(id, record)| (id.clone(), record.to_json())).collect());
}
if aggregate == "ConsoleSettings::Collection" {
    return Some(self.collection.entries().map(|(id, record)| (id.clone(), record.to_json())).collect());
}
if aggregate == "ConsoleSettings::Overview" {
    return Some(self.overview.entries().map(|(id, record)| (id.clone(), record.to_json())).collect());
}
        None
    }
}

pub fn dispatch_by_name(
    store: &mut Store,
    verb: &str,
    args_json: &crate::kernel::Json,
    caller_role: Option<&str>,
    caller_actor_id: Option<&str>,
    mutations: &mut Vec<crate::kernel::MutationRecord>,
) -> Result<Vec<crate::kernel::Event>, crate::kernel::Refusal> {
    match verb {
          "ConsoleSettings::StateStyle.Declare" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              if let Some(route) = route { route.require_depth(0)?; }
              let args = crate::generated::consolesettings::statestyle::DeclareArgs::from_json(facts_json)?;
                      args.agg.check_invariants()?;
                      args.state.check_invariants()?;
              crate::kernel::check_role_via(Some("Console"), "Declare", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = Vec::new();
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::statestyle::dispatch_declare(&mut store.statestyle, route, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::StateStyle.SetTone" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("SetToneArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["tone", "id", "state_style", "agg", "state"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "SetTone does not declare {} — it takes tone",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["tone"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "SetTone"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "tone"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::statestyle::StateStyle::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("SetTone acts on an existing StateStyle — pass agg.value, state.value:".to_string()))?, };
              let args = crate::generated::consolesettings::statestyle::SetToneArgs::from_json(facts_json)?;
              crate::kernel::check_role_via(Some("Console"), "SetTone", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::StateStyle", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::statestyle::dispatch_set_tone(&mut store.statestyle, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::StateStyle.SetAttention" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("SetAttentionArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["attention", "id", "state_style", "agg", "state"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "SetAttention does not declare {} — it takes attention",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["attention"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "SetAttention"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "attention"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::statestyle::StateStyle::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("SetAttention acts on an existing StateStyle — pass agg.value, state.value:".to_string()))?, };
              let args = crate::generated::consolesettings::statestyle::SetAttentionArgs::from_json(facts_json)?;
                      args.attention.check_invariants()?;
              crate::kernel::check_role_via(Some("Console"), "SetAttention", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::StateStyle", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::statestyle::dispatch_set_attention(&mut store.statestyle, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::StateStyle.SetExtra" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("SetExtraArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["extra_json", "id", "state_style", "agg", "state"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "SetExtra does not declare {} — it takes extra_json",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["extra_json"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "SetExtra"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "extra_json"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::statestyle::StateStyle::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("SetExtra acts on an existing StateStyle — pass agg.value, state.value:".to_string()))?, };
              let args = crate::generated::consolesettings::statestyle::SetExtraArgs::from_json(facts_json)?;
                      args.extra_json.check_invariants()?;
              crate::kernel::check_role_via(Some("Console"), "SetExtra", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::StateStyle", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::statestyle::dispatch_set_extra(&mut store.statestyle, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Collection.Declare" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              if let Some(route) = route { route.require_depth(0)?; }
              let args = crate::generated::consolesettings::collection::DeclareArgs::from_json(facts_json)?;
                      args.agg.check_invariants()?;
              crate::kernel::check_role_via(Some("Console"), "Declare", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = Vec::new();
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::collection::dispatch_declare(&mut store.collection, route, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Collection.SetLabel" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("SetLabelArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["label", "id", "collection", "agg"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "SetLabel does not declare {} — it takes label",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["label"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "SetLabel"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "label"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::collection::Collection::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("SetLabel acts on an existing Collection — pass agg.value:".to_string()))?, };
              let args = crate::generated::consolesettings::collection::SetLabelArgs::from_json(facts_json)?;
                      args.label.check_invariants()?;
              crate::kernel::check_role_via(Some("Console"), "SetLabel", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::Collection", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::collection::dispatch_set_label(&mut store.collection, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Collection.SetKey" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("SetKeyArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["key", "id", "collection", "agg"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "SetKey does not declare {} — it takes key",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["key"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "SetKey"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "key"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::collection::Collection::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("SetKey acts on an existing Collection — pass agg.value:".to_string()))?, };
              let args = crate::generated::consolesettings::collection::SetKeyArgs::from_json(facts_json)?;
                      args.key.check_invariants()?;
              crate::kernel::check_role_via(Some("Console"), "SetKey", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::Collection", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::collection::dispatch_set_key(&mut store.collection, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Collection.SetNavOrder" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("SetNavOrderArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["nav_order", "id", "collection", "agg"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "SetNavOrder does not declare {} — it takes nav_order",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["nav_order"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "SetNavOrder"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "nav_order"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::collection::Collection::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("SetNavOrder acts on an existing Collection — pass agg.value:".to_string()))?, };
              let args = crate::generated::consolesettings::collection::SetNavOrderArgs::from_json(facts_json)?;
                      args.nav_order.check_invariants()?;
              crate::kernel::check_role_via(Some("Console"), "SetNavOrder", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::Collection", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::collection::dispatch_set_nav_order(&mut store.collection, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Collection.SetPrimaryField" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("SetPrimaryFieldArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["primary_field", "id", "collection", "agg"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "SetPrimaryField does not declare {} — it takes primary_field",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["primary_field"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "SetPrimaryField"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "primary_field"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::collection::Collection::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("SetPrimaryField acts on an existing Collection — pass agg.value:".to_string()))?, };
              let args = crate::generated::consolesettings::collection::SetPrimaryFieldArgs::from_json(facts_json)?;
                      args.primary_field.check_invariants()?;
              crate::kernel::check_role_via(Some("Console"), "SetPrimaryField", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::Collection", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::collection::dispatch_set_primary_field(&mut store.collection, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Collection.SetListQuery" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("SetListQueryArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["list_query", "id", "collection", "agg"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "SetListQuery does not declare {} — it takes list_query",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["list_query"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "SetListQuery"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "list_query"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::collection::Collection::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("SetListQuery acts on an existing Collection — pass agg.value:".to_string()))?, };
              let args = crate::generated::consolesettings::collection::SetListQueryArgs::from_json(facts_json)?;
                      args.list_query.check_invariants()?;
              crate::kernel::check_role_via(Some("Console"), "SetListQuery", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::Collection", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::collection::dispatch_set_list_query(&mut store.collection, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Collection.SetIdentity" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("SetIdentityArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["identity_field", "identity_strategy", "identity_source", "identity_prefix", "identity_extra_json", "id", "collection", "agg"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "SetIdentity does not declare {} — it takes identity_field, identity_strategy, identity_source, identity_prefix, identity_extra_json",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["identity_field", "identity_strategy"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "SetIdentity"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "identity_field, identity_strategy, identity_source, identity_prefix, identity_extra_json"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::collection::Collection::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("SetIdentity acts on an existing Collection — pass agg.value:".to_string()))?, };
              let args = crate::generated::consolesettings::collection::SetIdentityArgs::from_json(facts_json)?;
                      args.identity_field.check_invariants()?;
                      if let Some(v) = &args.identity_source { v.check_invariants()?; }
                      if let Some(v) = &args.identity_prefix { v.check_invariants()?; }
                      if let Some(v) = &args.identity_extra_json { v.check_invariants()?; }
              crate::kernel::check_role_via(Some("Console"), "SetIdentity", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::Collection", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::collection::dispatch_set_identity(&mut store.collection, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Collection.SetExtra" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("SetExtraArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["extra_json", "id", "collection", "agg"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "SetExtra does not declare {} — it takes extra_json",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["extra_json"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "SetExtra"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "extra_json"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::collection::Collection::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("SetExtra acts on an existing Collection — pass agg.value:".to_string()))?, };
              let args = crate::generated::consolesettings::collection::SetExtraArgs::from_json(facts_json)?;
                      args.extra_json.check_invariants()?;
              crate::kernel::check_role_via(Some("Console"), "SetExtra", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::Collection", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::collection::dispatch_set_extra(&mut store.collection, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Collection.ReplaceColumns" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("ReplaceColumnsArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["columns", "id", "collection", "agg"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "ReplaceColumns does not declare {} — it takes columns",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["columns"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "ReplaceColumns"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "columns"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::collection::Collection::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("ReplaceColumns acts on an existing Collection — pass agg.value:".to_string()))?, };
              let args = crate::generated::consolesettings::collection::ReplaceColumnsArgs::from_json(facts_json)?;
                      for item in &args.columns { item.check_invariants()?; }
              crate::kernel::check_role_via(Some("Console"), "ReplaceColumns", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::Collection", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::collection::dispatch_replace_columns(&mut store.collection, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Collection.ReplaceDetailFields" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("ReplaceDetailFieldsArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["detail_fields", "id", "collection", "agg"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "ReplaceDetailFields does not declare {} — it takes detail_fields",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["detail_fields"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "ReplaceDetailFields"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "detail_fields"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::collection::Collection::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("ReplaceDetailFields acts on an existing Collection — pass agg.value:".to_string()))?, };
              let args = crate::generated::consolesettings::collection::ReplaceDetailFieldsArgs::from_json(facts_json)?;
                      for item in &args.detail_fields { item.check_invariants()?; }
              crate::kernel::check_role_via(Some("Console"), "ReplaceDetailFields", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::Collection", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::collection::dispatch_replace_detail_fields(&mut store.collection, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Collection.ReplaceDetailFieldColumns" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("ReplaceDetailFieldColumnsArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["detail_field_columns", "id", "collection", "agg"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "ReplaceDetailFieldColumns does not declare {} — it takes detail_field_columns",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["detail_field_columns"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "ReplaceDetailFieldColumns"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "detail_field_columns"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::collection::Collection::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("ReplaceDetailFieldColumns acts on an existing Collection — pass agg.value:".to_string()))?, };
              let args = crate::generated::consolesettings::collection::ReplaceDetailFieldColumnsArgs::from_json(facts_json)?;
                      for item in &args.detail_field_columns { item.check_invariants()?; }
              crate::kernel::check_role_via(Some("Console"), "ReplaceDetailFieldColumns", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::Collection", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::collection::dispatch_replace_detail_field_columns(&mut store.collection, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Collection.ReplacePreconditions" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("ReplacePreconditionsArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["preconditions", "id", "collection", "agg"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "ReplacePreconditions does not declare {} — it takes preconditions",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["preconditions"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "ReplacePreconditions"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "preconditions"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::collection::Collection::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("ReplacePreconditions acts on an existing Collection — pass agg.value:".to_string()))?, };
              let args = crate::generated::consolesettings::collection::ReplacePreconditionsArgs::from_json(facts_json)?;
                      for item in &args.preconditions { item.check_invariants()?; }
              crate::kernel::check_role_via(Some("Console"), "ReplacePreconditions", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::Collection", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::collection::dispatch_replace_preconditions(&mut store.collection, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Collection.ReplaceFieldFormats" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("ReplaceFieldFormatsArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["field_formats", "id", "collection", "agg"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "ReplaceFieldFormats does not declare {} — it takes field_formats",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["field_formats"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "ReplaceFieldFormats"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "field_formats"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::collection::Collection::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("ReplaceFieldFormats acts on an existing Collection — pass agg.value:".to_string()))?, };
              let args = crate::generated::consolesettings::collection::ReplaceFieldFormatsArgs::from_json(facts_json)?;
                      for item in &args.field_formats { item.check_invariants()?; }
              crate::kernel::check_role_via(Some("Console"), "ReplaceFieldFormats", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::Collection", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::collection::dispatch_replace_field_formats(&mut store.collection, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Overview.Declare" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              if let Some(route) = route { route.require_depth(0)?; }
              let args = crate::generated::consolesettings::overview::DeclareArgs::from_json(facts_json)?;
                      args.key.check_invariants()?;
              crate::kernel::check_role_via(Some("Console"), "Declare", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = Vec::new();
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::overview::dispatch_declare(&mut store.overview, route, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "ConsoleSettings::Overview.ReplaceStats" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("ReplaceStatsArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["stats", "id", "overview", "key"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "ReplaceStats does not declare {} — it takes stats",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["stats"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "ReplaceStats"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "stats"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::consolesettings::overview::Overview::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("ReplaceStats acts on an existing Overview — pass key.value:".to_string()))?, };
              let args = crate::generated::consolesettings::overview::ReplaceStatsArgs::from_json(facts_json)?;
                      for item in &args.stats { item.check_invariants()?; }
              crate::kernel::check_role_via(Some("Console"), "ReplaceStats", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "ConsoleSettings::Overview", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::consolesettings::overview::dispatch_replace_stats(&mut store.overview, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
        other => Err(crate::kernel::Refusal::TypeMismatch(format!("unknown command {other:?}"))),
    }
}

/// Ruby's own event payload is `payload: args` — the WHOLE hash the
/// caller passed to `dispatch`, not filtered to the command's own
/// declared attributes (an identity-reference argument like
/// `number:` is not a declared `CreditArgs` field, and still shows
/// up on `AccountCredited`'s payload). `args.to_json()` inside each
/// generated `dispatch_*` only ever sees the NARROWED, typed args
/// struct, so it structurally can't reproduce that — this replaces
/// whatever payload the generated dispatch built with the raw,
/// unfiltered `args_json` this router itself received, after the
/// fact, once dispatch has already decided whether to accept it.
/// Load-bearing for process managers, not just payload fidelity: a
/// saga leg that forwards `reference: :reference` into `Account.
/// Debit` (which doesn't declare a `reference` attribute) needs
/// that field to survive onto `AccountDebited`'s own payload for
/// `correlates_by` to find it downstream.
fn stamp_payload(events: Vec<crate::kernel::Event>, args_json: &crate::kernel::Json) -> Vec<crate::kernel::Event> {
    events.into_iter().map(|e| crate::kernel::Event { payload: args_json.clone(), ..e }).collect()
}

pub static REFERENCE_TABLE: crate::kernel::ReferenceTable = &[
    ("ConsoleSettings::StateStyle", &[]),
    ("ConsoleSettings::Collection", &[]),
    ("ConsoleSettings::Overview", &[]),
];

impl crate::kernel::ReferenceLookup for Store {
    fn find_fielded(&self, target: &str, id: &str) -> Option<Box<dyn crate::kernel::Fielded>> {
if target == "ConsoleSettings::StateStyle" {
    return self.statestyle.find(id).map(|r| Box::new(r) as Box<dyn crate::kernel::Fielded>);
}
if target == "ConsoleSettings::Collection" {
    return self.collection.find(id).map(|r| Box::new(r) as Box<dyn crate::kernel::Fielded>);
}
if target == "ConsoleSettings::Overview" {
    return self.overview.find(id).map(|r| Box::new(r) as Box<dyn crate::kernel::Fielded>);
}
        None
    }
}

pub const POLICIES: &[crate::kernel::PolicyRule] = &[

];

pub const CROSS_DOMAIN_POLICIES: &[crate::kernel::CrossDomainPolicyRule] = &[

];



pub const PROCESS_MANAGERS: &[crate::kernel::ProcessManagerDef] = &[

];

pub fn reference_key_for_aggregate(qualified_name: &str) -> Option<&'static str> {
    match qualified_name {
        "ConsoleSettings::StateStyle" => Some("state_style"),
        "ConsoleSettings::Collection" => Some("collection"),
        "ConsoleSettings::Overview" => Some("overview"),
        _ => None,
    }
}

pub fn command_creates(verb: &str) -> bool {
    match verb {
        "ConsoleSettings::StateStyle.Declare" => true,
        "ConsoleSettings::StateStyle.SetTone" => false,
        "ConsoleSettings::StateStyle.SetAttention" => false,
        "ConsoleSettings::StateStyle.SetExtra" => false,
        "ConsoleSettings::Collection.Declare" => true,
        "ConsoleSettings::Collection.SetLabel" => false,
        "ConsoleSettings::Collection.SetKey" => false,
        "ConsoleSettings::Collection.SetNavOrder" => false,
        "ConsoleSettings::Collection.SetPrimaryField" => false,
        "ConsoleSettings::Collection.SetListQuery" => false,
        "ConsoleSettings::Collection.SetIdentity" => false,
        "ConsoleSettings::Collection.SetExtra" => false,
        "ConsoleSettings::Collection.ReplaceColumns" => false,
        "ConsoleSettings::Collection.ReplaceDetailFields" => false,
        "ConsoleSettings::Collection.ReplaceDetailFieldColumns" => false,
        "ConsoleSettings::Collection.ReplacePreconditions" => false,
        "ConsoleSettings::Collection.ReplaceFieldFormats" => false,
        "ConsoleSettings::Overview.Declare" => true,
        "ConsoleSettings::Overview.ReplaceStats" => false,
        _ => false,
    }
}

pub fn identity_head_for_aggregate(qualified_name: &str) -> Option<&'static str> {
    match qualified_name {
        "ConsoleSettings::Collection" => Some("agg"),
        "ConsoleSettings::Overview" => Some("key"),
        _ => None,
    }
}

pub fn entity_identity_head_for_path(qualified_path: &str) -> Option<&'static str> {
    match qualified_path {

        _ => None,
    }
}

pub fn command_attributes_for_verb(verb: &str) -> &'static [&'static str] {
    match verb {
        "ConsoleSettings::StateStyle.Declare" => &["agg", "state"],
        "ConsoleSettings::StateStyle.SetTone" => &["tone"],
        "ConsoleSettings::StateStyle.SetAttention" => &["attention"],
        "ConsoleSettings::StateStyle.SetExtra" => &["extra_json"],
        "ConsoleSettings::Collection.Declare" => &["agg"],
        "ConsoleSettings::Collection.SetLabel" => &["label"],
        "ConsoleSettings::Collection.SetKey" => &["key"],
        "ConsoleSettings::Collection.SetNavOrder" => &["nav_order"],
        "ConsoleSettings::Collection.SetPrimaryField" => &["primary_field"],
        "ConsoleSettings::Collection.SetListQuery" => &["list_query"],
        "ConsoleSettings::Collection.SetIdentity" => &["identity_field", "identity_strategy", "identity_source", "identity_prefix", "identity_extra_json"],
        "ConsoleSettings::Collection.SetExtra" => &["extra_json"],
        "ConsoleSettings::Collection.ReplaceColumns" => &["columns"],
        "ConsoleSettings::Collection.ReplaceDetailFields" => &["detail_fields"],
        "ConsoleSettings::Collection.ReplaceDetailFieldColumns" => &["detail_field_columns"],
        "ConsoleSettings::Collection.ReplacePreconditions" => &["preconditions"],
        "ConsoleSettings::Collection.ReplaceFieldFormats" => &["field_formats"],
        "ConsoleSettings::Overview.Declare" => &["key"],
        "ConsoleSettings::Overview.ReplaceStats" => &["stats"],
        _ => &[],
    }
}

pub const QUERIES: &[crate::kernel::QueryDef] = &[

];
/// `provides "authorization", assignments:` — the query `kernel::check_role_via` reads; `None` when no chapter here declares one.
pub const AUTHORIZATION_ASSIGNMENTS: Option<&str> = None;
/// Declared entity queries (`Aggregate.Entity.Query`) — `kernel::named_query::run_entity`.
pub const ENTITY_QUERIES: &[crate::kernel::named_query::EntityQueryDef] = &[
];

/// C3.7 for a named query's own arguments — `query_arg_checks`
/// (rust/project/queries.rb) has the full story.
pub fn check_query_args(verb: &str, args: &crate::kernel::Json) -> Result<(), crate::kernel::Refusal> {
    match verb {

        _ => Ok(()),
    }
}

pub fn group_by_styles(rows: Vec<(String, crate::kernel::Json)>) -> crate::kernel::Json {
    let unwrapped: Vec<crate::kernel::Json> = rows
        .into_iter()
        .map(|(id, record)| {
            let wrapped = crate::kernel::repository::row_json(id, record);
            match wrapped {
                crate::kernel::Json::Object(fields) => crate::kernel::Json::Object(
                    fields
                        .into_iter()
                        .filter(|(k, _)| k == "agg" || k == "state" || k == "tone" || k == "attention" || k == "extra_json" || k == "id")
                        .map(|(k, v)| {
                            let new_v = match k.as_str() {
            "agg" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "state" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "tone" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "attention" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "extra_json" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                                _ => v,
                            };
                            (k, new_v)
                        })
                        .collect(),
                ),
                other => other,
            }
        })
        .collect();
    crate::kernel::read_model::nest(unwrapped, &["agg", "state"])
}

pub fn group_by_curated(rows: Vec<(String, crate::kernel::Json)>) -> crate::kernel::Json {
    let unwrapped: Vec<crate::kernel::Json> = rows
        .into_iter()
        .map(|(id, record)| {
            let wrapped = crate::kernel::repository::row_json(id, record);
            match wrapped {
                crate::kernel::Json::Object(fields) => crate::kernel::Json::Object(
                    fields
                        .into_iter()
                        .filter(|(k, _)| k == "agg" || k == "label" || k == "key" || k == "nav_order" || k == "primary_field" || k == "list_query" || k == "identity_field" || k == "identity_strategy" || k == "identity_source" || k == "identity_prefix" || k == "identity_extra_json" || k == "columns" || k == "detail_fields" || k == "detail_field_columns" || k == "preconditions" || k == "field_formats" || k == "extra_json" || k == "id")
                        .map(|(k, v)| {
                            let new_v = match k.as_str() {
            "agg" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "label" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "key" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "nav_order" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "primary_field" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "list_query" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "identity_field" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "identity_strategy" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "identity_source" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "identity_prefix" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "identity_extra_json" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                    "columns" => match v { crate::kernel::Json::Array(items) => crate::kernel::Json::Array(items.into_iter().map(|item| match item { crate::kernel::Json::Object(fields) => crate::kernel::Json::Object(fields.into_iter().map(|(k, v)| { let new_v = match k.as_str() { "field" => v, "sortable" => v, "sort_default" => v, "extra_json" => v, _ => v }; (k, new_v) }).collect()), other => other }).collect()), other => other },
                    "detail_fields" => match v { crate::kernel::Json::Array(items) => crate::kernel::Json::Array(items.into_iter().map(|item| match item { crate::kernel::Json::Object(fields) => crate::kernel::Json::Object(fields.into_iter().map(|(k, v)| { let new_v = match k.as_str() { "field" => v, "display" => v, "state_filter" => v, "extra_json" => v, _ => v }; (k, new_v) }).collect()), other => other }).collect()), other => other },
                    "detail_field_columns" => match v { crate::kernel::Json::Array(items) => crate::kernel::Json::Array(items.into_iter().map(|item| match item { crate::kernel::Json::Object(fields) => crate::kernel::Json::Object(fields.into_iter().map(|(k, v)| { let new_v = match k.as_str() { "field" => v, "column" => v, _ => v }; (k, new_v) }).collect()), other => other }).collect()), other => other },
                    "preconditions" => match v { crate::kernel::Json::Array(items) => crate::kernel::Json::Array(items.into_iter().map(|item| match item { crate::kernel::Json::Object(fields) => crate::kernel::Json::Object(fields.into_iter().map(|(k, v)| { let new_v = match k.as_str() { "field" => v, "state" => v, "extra_json" => v, _ => v }; (k, new_v) }).collect()), other => other }).collect()), other => other },
                    "field_formats" => match v { crate::kernel::Json::Array(items) => crate::kernel::Json::Array(items.into_iter().map(|item| match item { crate::kernel::Json::Object(fields) => crate::kernel::Json::Object(fields.into_iter().map(|(k, v)| { let new_v = match k.as_str() { "field" => v, "format" => v, _ => v }; (k, new_v) }).collect()), other => other }).collect()), other => other },
                    "extra_json" => match v { crate::kernel::Json::Object(fields) => fields.into_iter().find(|(k, _)| k == "value").map(|(_, field_value)| field_value).unwrap_or(crate::kernel::Json::Null), other => other },
                                _ => v,
                            };
                            (k, new_v)
                        })
                        .collect(),
                ),
                other => other,
            }
        })
        .collect();
    crate::kernel::read_model::nest(unwrapped, &["agg"])
}

pub const READ_MODELS: &[crate::kernel::read_model::ReadModelDef] = &[
crate::kernel::read_model::ReadModelDef {
    verb: "ConsoleSettings.Styles",
    reference_name: None,
    heads: &[
        crate::kernel::read_model::ReadModelHead { aggregate: "ConsoleSettings::StateStyle", as_name: "state_styles", many: true, is_root: false, reference_fields: &[] },
    ],
    filtered_head: None,
    conditions: &[

    ],
    reference_hop_conditions: &[

    ],
    order_by: None,
    offset: None,
    limit: None,
    authorization: None,
    group_by: Some(group_by_styles),
    count: false,
    median_field: None,
},
crate::kernel::read_model::ReadModelDef {
    verb: "ConsoleSettings.Curated",
    reference_name: None,
    heads: &[
        crate::kernel::read_model::ReadModelHead { aggregate: "ConsoleSettings::Collection", as_name: "collections", many: true, is_root: false, reference_fields: &[] },
    ],
    filtered_head: None,
    conditions: &[

    ],
    reference_hop_conditions: &[

    ],
    order_by: None,
    offset: None,
    limit: None,
    authorization: None,
    group_by: Some(group_by_curated),
    count: false,
    median_field: None,
},
];
