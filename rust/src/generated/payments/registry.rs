// GENERATED by bin/project_rust — the JSON command router
// `kernel::cli` dispatches every step through. Do not hand-edit —
// re-run bin/project_rust instead.
#![allow(dead_code, unused_variables)]

// `Repository::save` (from_seed, below) is a TRAIT method —
// `InMemoryRepository`'s own inherent methods (entries(), used
// by instances()) need no import, but save() does.
use crate::kernel::Repository;

// `Clone` — a DRY RUN (`cli::run`'s `{"dry_run": …}` step, `cli::serve`'s
// `snapshot`/`restore`) dispatches against a throwaway copy of the whole
// store: givens, mutations, ensures all run for real, nothing is kept.
#[derive(Clone)]
pub struct Store {
    pub payment: crate::kernel::InMemoryRepository<crate::generated::payments::payment::Payment>,
}

impl Store {
    pub fn new() -> Self {
        Self {
            payment: crate::kernel::InMemoryRepository::new(),
        }
    }

    /// Every aggregate this domain declared, dumped as
    /// "Domain::Aggregate#id" -> its own to_json() — the exact key
    /// shape bin/rust_conformance's own comparable["instances"]
    /// builds from Ruby (Fuzzing::Replay.call's own instance key
    /// format, read directly).
    pub fn instances(&self) -> Vec<(String, crate::kernel::Json)> {
        let mut instances = Vec::new();
for (id, record) in self.payment.entries() {
    instances.push((format!("{}{}", "Payments::Payment#", id), record.to_json()));
}
        instances
    }

    /// Seeds a fresh `Store` from a prior `instances()` dump —
    /// the exact "Domain::Aggregate#id" -> state shape, read
    /// back instead of written. Not `Result`-returning on a
    /// non-Object `seed`: an absent/malformed seed just yields
    /// an empty Store, the same starting point `Store::new()`
    /// already gives a caller with no prior state to seed from.
    pub fn from_seed(seed: &crate::kernel::Json) -> Result<Self, crate::kernel::Refusal> {
        let mut store = Self::new();
        if let crate::kernel::Json::Object(fields) = seed {
            for (key, value) in fields {
if let Some(id) = key.strip_prefix("Payments::Payment#") {
    store.payment.save(id, crate::generated::payments::payment::Payment::from_json(value)?);
    continue;
}
            }
        }
        Ok(store)
    }
}

/// `AggregateScan` — kernel/repository.rs's own trait, given a REAL
/// per-aggregate body here: one `if` per aggregate this domain declared,
/// the same "Domain::Aggregate" prefix `instances()`'s own dump arms
/// already use, each returning that ONE aggregate's own (id, to_json())
/// listing straight off its repository's `entries()`. Falls through to
/// the trait's own default (`None`) for any prefix that matches none of
/// them — kernel/cli.rs turns that into a clean "unknown aggregate"
/// refusal, never a panic.
impl crate::kernel::AggregateScan for Store {
    fn scan(&self, aggregate: &str) -> Option<Vec<(String, crate::kernel::Json)>> {
if aggregate == "Payments::Payment" {
    return Some(self.payment.entries().map(|(id, record)| (id.clone(), record.to_json())).collect());
}
        None
    }
}

pub fn dispatch_by_name(
    store: &mut Store,
    verb: &str,
    args_json: &crate::kernel::Json,
    caller_role: Option<&str>,
    caller_actor_id: Option<&str>,
    mutations: &mut Vec<crate::kernel::MutationRecord>,
) -> Result<Vec<crate::kernel::Event>, crate::kernel::Refusal> {
    match verb {
          "Payments::Payment.Initiate" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              if let Some(route) = route { route.require_depth(0)?; }
              let args = crate::generated::payments::payment::InitiateArgs::from_json(facts_json)?;
                      args.reference.check_invariants()?;
                      args.amount.check_invariants()?;
                      args.client.check_invariants()?;
                      if let Some(v) = &args.customer_id { v.check_invariants()?; }
              crate::kernel::check_role_via(Some("Merchant"), "Initiate", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = Vec::new();
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_initiate(&mut store.payment, route, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Payments::Payment.Succeed" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("SucceedArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["transaction_id", "reported_processor", "id", "payment", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Succeed does not declare {} — it takes transaction_id, reported_processor",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["reported_processor", "transaction_id"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "Succeed"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "transaction_id, reported_processor"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::payments::payment::Payment::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("Succeed acts on an existing Payment — pass reference.value:".to_string()))?, };
              let args = crate::generated::payments::payment::SucceedArgs::from_json(facts_json)?;
                      args.transaction_id.check_invariants()?;
              crate::kernel::check_role_via(Some("System"), "Succeed", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Payments::Payment", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_succeed(&mut store.payment, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Payments::Payment.Fail" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("FailArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["reason", "reported_processor", "id", "payment", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Fail does not declare {} — it takes reason, reported_processor",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["reason", "reported_processor"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "Fail"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "reason, reported_processor"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::payments::payment::Payment::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("Fail acts on an existing Payment — pass reference.value:".to_string()))?, };
              let args = crate::generated::payments::payment::FailArgs::from_json(facts_json)?;
                      args.reason.check_invariants()?;
              crate::kernel::check_role_via(Some("System"), "Fail", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Payments::Payment", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_fail(&mut store.payment, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Payments::Payment.Refund" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("RefundArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["refund_amount", "id", "payment", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Refund does not declare {} — it takes refund_amount",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["refund_amount"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "Refund"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "refund_amount"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::payments::payment::Payment::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("Refund acts on an existing Payment — pass reference.value:".to_string()))?, };
              let args = crate::generated::payments::payment::RefundArgs::from_json(facts_json)?;
                      args.refund_amount.check_invariants()?;
              crate::kernel::check_role_via(Some("Support"), "Refund", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Payments::Payment", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_refund(&mut store.payment, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Payments::Payment.CompleteRefund" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("CompleteRefundArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["confirmed_amount", "refund_transaction_id", "reported_processor", "id", "payment", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "CompleteRefund does not declare {} — it takes confirmed_amount, refund_transaction_id, reported_processor",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["confirmed_amount", "refund_transaction_id", "reported_processor"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "CompleteRefund"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "confirmed_amount, refund_transaction_id, reported_processor"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::payments::payment::Payment::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("CompleteRefund acts on an existing Payment — pass reference.value:".to_string()))?, };
              let args = crate::generated::payments::payment::CompleteRefundArgs::from_json(facts_json)?;
                      args.confirmed_amount.check_invariants()?;
                      args.refund_transaction_id.check_invariants()?;
              crate::kernel::check_role_via(Some("System"), "CompleteRefund", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Payments::Payment", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_complete_refund(&mut store.payment, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Payments::Payment.RecordPartialRefund" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("RecordPartialRefundArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["confirmed_amount", "refund_transaction_id", "reported_processor", "id", "payment", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "RecordPartialRefund does not declare {} — it takes confirmed_amount, refund_transaction_id, reported_processor",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["confirmed_amount", "refund_transaction_id", "reported_processor"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "RecordPartialRefund"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "confirmed_amount, refund_transaction_id, reported_processor"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::payments::payment::Payment::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("RecordPartialRefund acts on an existing Payment — pass reference.value:".to_string()))?, };
              let args = crate::generated::payments::payment::RecordPartialRefundArgs::from_json(facts_json)?;
                      args.confirmed_amount.check_invariants()?;
                      args.refund_transaction_id.check_invariants()?;
              crate::kernel::check_role_via(Some("System"), "RecordPartialRefund", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Payments::Payment", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_record_partial_refund(&mut store.payment, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Payments::Payment.DeclineRefund" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("DeclineRefundArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["reason", "reported_processor", "id", "payment", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "DeclineRefund does not declare {} — it takes reason, reported_processor",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["reason", "reported_processor"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "DeclineRefund"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "reason, reported_processor"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::payments::payment::Payment::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("DeclineRefund acts on an existing Payment — pass reference.value:".to_string()))?, };
              let args = crate::generated::payments::payment::DeclineRefundArgs::from_json(facts_json)?;
                      args.reason.check_invariants()?;
              crate::kernel::check_role_via(Some("System"), "DeclineRefund", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Payments::Payment", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_decline_refund(&mut store.payment, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Payments::Payment.OpenDispute" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("OpenDisputeArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["dispute_id", "reason", "reported_processor", "id", "payment", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "OpenDispute does not declare {} — it takes dispute_id, reason, reported_processor",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["dispute_id", "reason", "reported_processor"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "OpenDispute"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "dispute_id, reason, reported_processor"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::payments::payment::Payment::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("OpenDispute acts on an existing Payment — pass reference.value:".to_string()))?, };
              let args = crate::generated::payments::payment::OpenDisputeArgs::from_json(facts_json)?;
                      args.dispute_id.check_invariants()?;
                      args.reason.check_invariants()?;
              crate::kernel::check_role_via(Some("System"), "OpenDispute", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Payments::Payment", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_open_dispute(&mut store.payment, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Payments::Payment.WinDispute" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("WinDisputeArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["reported_processor", "id", "payment", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "WinDispute does not declare {} — it takes reported_processor",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["reported_processor"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "WinDispute"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "reported_processor"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::payments::payment::Payment::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("WinDispute acts on an existing Payment — pass reference.value:".to_string()))?, };
              let args = crate::generated::payments::payment::WinDisputeArgs::from_json(facts_json)?;
              crate::kernel::check_role_via(Some("System"), "WinDispute", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Payments::Payment", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_win_dispute(&mut store.payment, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Payments::Payment.Chargeback" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let route = invocation.route();
              let facts_json = invocation.facts();
              { let v = facts_json; if !matches!(v, crate::kernel::Json::Object(_)) {
    return Err(crate::kernel::Refusal::TypeMismatch(format!("ChargebackArgs expects an object, got {}", v.inspect())));
}
let unknown = v.unknown_keys(&["reported_processor", "id", "payment", "reference"]);
if !unknown.is_empty() {
    return Err(crate::kernel::Refusal::UnknownArgument(format!(
        "Chargeback does not declare {} — it takes reported_processor",
        unknown.join(", ")
    )));
}
let absent: Vec<&str> = ["reported_processor"].into_iter().filter(|key| v.get(key).is_none()).collect();
if !absent.is_empty() {
    return Err(crate::kernel::Refusal::AbsentArgument(crate::kernel::RefusalSite::AbsentArgumentAbsentArgs.render(&[
        ("command", "Chargeback"),
        ("absent", absent.join(", ").as_str()),
        ("declared", "reported_processor"),
    ])));
}
 }
              let id = match route { Some(route) => { route.require_depth(0)?; route.aggregate().to_string() }, None => crate::generated::payments::payment::Payment::extract_id(facts_json).map_err(|_| crate::kernel::Refusal::NotFound("Chargeback acts on an existing Payment — pass reference.value:".to_string()))?, };
              let args = crate::generated::payments::payment::ChargebackArgs::from_json(facts_json)?;
              crate::kernel::check_role_via(Some("System"), "Chargeback", caller_role, caller_actor_id, &*store, QUERIES, AUTHORIZATION_ASSIGNMENTS)?;
              let tenant_boundary_check: Result<(), crate::kernel::Refusal> = Ok(());
              let owner_deref = crate::kernel::owner_deref(&*store, REFERENCE_TABLE, "Payments::Payment", &id);
              let command_deref = crate::kernel::command_deref(&*store, REFERENCE_TABLE, &[], &args);
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_chargeback(&mut store.payment, &id, args, mutations, owner_deref, command_deref, tenant_boundary_check).map(|(_, events)| stamp_payload(events, &payload))
          }
          "Payments::Payment.PaymentGateway.Succeeded" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let (id, port_facts) = invocation.split_aggregate_receiver(None, Some("reference"))?;
              let facts_json = &port_facts;
              let _instance = store.payment.find(&id).ok_or_else(|| crate::kernel::Refusal::NotFound(format!("Payment {:?} does not exist", id)))?;
              let args = crate::generated::payments::payment::PaymentGatewaySucceededArgs::from_json(facts_json)?;
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_operation_paymentgateway_succeeded(&id, args).map(|events| stamp_payload(events, &payload))
          }
          "Payments::Payment.PaymentGateway.Failed" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let (id, port_facts) = invocation.split_aggregate_receiver(None, Some("reference"))?;
              let facts_json = &port_facts;
              let _instance = store.payment.find(&id).ok_or_else(|| crate::kernel::Refusal::NotFound(format!("Payment {:?} does not exist", id)))?;
              let args = crate::generated::payments::payment::PaymentGatewayFailedArgs::from_json(facts_json)?;
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_operation_paymentgateway_failed(&id, args).map(|events| stamp_payload(events, &payload))
          }
          "Payments::Payment.PaymentGateway.Refunded" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let (id, port_facts) = invocation.split_aggregate_receiver(None, Some("reference"))?;
              let facts_json = &port_facts;
              let _instance = store.payment.find(&id).ok_or_else(|| crate::kernel::Refusal::NotFound(format!("Payment {:?} does not exist", id)))?;
              let args = crate::generated::payments::payment::PaymentGatewayRefundedArgs::from_json(facts_json)?;
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_operation_paymentgateway_refunded(&id, args).map(|events| stamp_payload(events, &payload))
          }
          "Payments::Payment.PaymentGateway.RefundDeclined" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let (id, port_facts) = invocation.split_aggregate_receiver(None, Some("reference"))?;
              let facts_json = &port_facts;
              let _instance = store.payment.find(&id).ok_or_else(|| crate::kernel::Refusal::NotFound(format!("Payment {:?} does not exist", id)))?;
              let args = crate::generated::payments::payment::PaymentGatewayRefundDeclinedArgs::from_json(facts_json)?;
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_operation_paymentgateway_refund_declined(&id, args).map(|events| stamp_payload(events, &payload))
          }
          "Payments::Payment.PaymentGateway.DisputeOpened" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let (id, port_facts) = invocation.split_aggregate_receiver(None, Some("reference"))?;
              let facts_json = &port_facts;
              let _instance = store.payment.find(&id).ok_or_else(|| crate::kernel::Refusal::NotFound(format!("Payment {:?} does not exist", id)))?;
              let args = crate::generated::payments::payment::PaymentGatewayDisputeOpenedArgs::from_json(facts_json)?;
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_operation_paymentgateway_dispute_opened(&id, args).map(|events| stamp_payload(events, &payload))
          }
          "Payments::Payment.PaymentGateway.DisputeWon" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let (id, port_facts) = invocation.split_aggregate_receiver(None, Some("reference"))?;
              let facts_json = &port_facts;
              let _instance = store.payment.find(&id).ok_or_else(|| crate::kernel::Refusal::NotFound(format!("Payment {:?} does not exist", id)))?;
              let args = crate::generated::payments::payment::PaymentGatewayDisputeWonArgs::from_json(facts_json)?;
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_operation_paymentgateway_dispute_won(&id, args).map(|events| stamp_payload(events, &payload))
          }
          "Payments::Payment.PaymentGateway.DisputeLost" => {
              let invocation = crate::kernel::CommandInvocation::from_json(args_json)?;
              let (id, port_facts) = invocation.split_aggregate_receiver(None, Some("reference"))?;
              let facts_json = &port_facts;
              let _instance = store.payment.find(&id).ok_or_else(|| crate::kernel::Refusal::NotFound(format!("Payment {:?} does not exist", id)))?;
              let args = crate::generated::payments::payment::PaymentGatewayDisputeLostArgs::from_json(facts_json)?;
              let payload = crate::kernel::Json::overlay(facts_json, &args.to_json());
              crate::generated::payments::payment::dispatch_operation_paymentgateway_dispute_lost(&id, args).map(|events| stamp_payload(events, &payload))
          }
        other => Err(crate::kernel::Refusal::TypeMismatch(format!("unknown command {other:?}"))),
    }
}

/// Ruby's own event payload is `payload: args` — the WHOLE hash the
/// caller passed to `dispatch`, not filtered to the command's own
/// declared attributes (an identity-reference argument like
/// `number:` is not a declared `CreditArgs` field, and still shows
/// up on `AccountCredited`'s payload). `args.to_json()` inside each
/// generated `dispatch_*` only ever sees the NARROWED, typed args
/// struct, so it structurally can't reproduce that — this replaces
/// whatever payload the generated dispatch built with the raw,
/// unfiltered `args_json` this router itself received, after the
/// fact, once dispatch has already decided whether to accept it.
/// Load-bearing for process managers, not just payload fidelity: a
/// saga leg that forwards `reference: :reference` into `Account.
/// Debit` (which doesn't declare a `reference` attribute) needs
/// that field to survive onto `AccountDebited`'s own payload for
/// `correlates_by` to find it downstream.
fn stamp_payload(events: Vec<crate::kernel::Event>, args_json: &crate::kernel::Json) -> Vec<crate::kernel::Event> {
    events.into_iter().map(|e| crate::kernel::Event { payload: args_json.clone(), ..e }).collect()
}

pub static REFERENCE_TABLE: crate::kernel::ReferenceTable = &[
    ("Payments::Payment", &[]),
];

impl crate::kernel::ReferenceLookup for Store {
    fn find_fielded(&self, target: &str, id: &str) -> Option<Box<dyn crate::kernel::Fielded>> {
if target == "Payments::Payment" {
    return self.payment.find(id).map(|r| Box::new(r) as Box<dyn crate::kernel::Fielded>);
}
        None
    }
}

pub const POLICIES: &[crate::kernel::PolicyRule] = &[
    crate::kernel::PolicyRule { policy_name: "OnPaymentConfirmedByProcessor", event_name: "PaymentConfirmedByProcessor", event_qualifier: None, target_verb: "Payments::Payment.Succeed", for_each: None, for_each_key: None, with_spec: &[], where_expr: None },
    crate::kernel::PolicyRule { policy_name: "OnPaymentDeclinedByProcessor", event_name: "PaymentDeclinedByProcessor", event_qualifier: None, target_verb: "Payments::Payment.Fail", for_each: None, for_each_key: None, with_spec: &[], where_expr: None },
    crate::kernel::PolicyRule { policy_name: "OnPaymentRefundConfirmedByProcessor", event_name: "PaymentRefundConfirmedByProcessor", event_qualifier: None, target_verb: "Payments::Payment.CompleteRefund", for_each: None, for_each_key: None, with_spec: &[], where_expr: None },
    crate::kernel::PolicyRule { policy_name: "OnPaymentRefundPartiallyConfirmedByProcessor", event_name: "PaymentRefundConfirmedByProcessor", event_qualifier: None, target_verb: "Payments::Payment.RecordPartialRefund", for_each: None, for_each_key: None, with_spec: &[], where_expr: None },
    crate::kernel::PolicyRule { policy_name: "OnPaymentRefundDeclinedByProcessor", event_name: "PaymentRefundDeclinedByProcessor", event_qualifier: None, target_verb: "Payments::Payment.DeclineRefund", for_each: None, for_each_key: None, with_spec: &[], where_expr: None },
    crate::kernel::PolicyRule { policy_name: "OnPaymentDisputeOpenedByProcessor", event_name: "PaymentDisputeOpenedByProcessor", event_qualifier: None, target_verb: "Payments::Payment.OpenDispute", for_each: None, for_each_key: None, with_spec: &[], where_expr: None },
    crate::kernel::PolicyRule { policy_name: "OnPaymentDisputeWonByProcessor", event_name: "PaymentDisputeWonByProcessor", event_qualifier: None, target_verb: "Payments::Payment.WinDispute", for_each: None, for_each_key: None, with_spec: &[], where_expr: None },
    crate::kernel::PolicyRule { policy_name: "OnPaymentDisputeLostByProcessor", event_name: "PaymentDisputeLostByProcessor", event_qualifier: None, target_verb: "Payments::Payment.Chargeback", for_each: None, for_each_key: None, with_spec: &[], where_expr: None },
];

pub const CROSS_DOMAIN_POLICIES: &[crate::kernel::CrossDomainPolicyRule] = &[

];



pub const PROCESS_MANAGERS: &[crate::kernel::ProcessManagerDef] = &[

];

pub fn reference_key_for_aggregate(qualified_name: &str) -> Option<&'static str> {
    match qualified_name {
        "Payments::Payment" => Some("payment"),
        _ => None,
    }
}

pub fn command_creates(verb: &str) -> bool {
    match verb {
        "Payments::Payment.Initiate" => true,
        "Payments::Payment.Succeed" => false,
        "Payments::Payment.Fail" => false,
        "Payments::Payment.Refund" => false,
        "Payments::Payment.CompleteRefund" => false,
        "Payments::Payment.RecordPartialRefund" => false,
        "Payments::Payment.DeclineRefund" => false,
        "Payments::Payment.OpenDispute" => false,
        "Payments::Payment.WinDispute" => false,
        "Payments::Payment.Chargeback" => false,
        _ => false,
    }
}

pub fn identity_head_for_aggregate(qualified_name: &str) -> Option<&'static str> {
    match qualified_name {
        "Payments::Payment" => Some("reference"),
        _ => None,
    }
}

pub fn entity_identity_head_for_path(qualified_path: &str) -> Option<&'static str> {
    match qualified_path {

        _ => None,
    }
}

pub fn command_attributes_for_verb(verb: &str) -> &'static [&'static str] {
    match verb {
        "Payments::Payment.Initiate" => &["reference", "processor", "payment_type", "amount", "client", "customer_id"],
        "Payments::Payment.Succeed" => &["transaction_id", "reported_processor"],
        "Payments::Payment.Fail" => &["reason", "reported_processor"],
        "Payments::Payment.Refund" => &["refund_amount"],
        "Payments::Payment.CompleteRefund" => &["confirmed_amount", "refund_transaction_id", "reported_processor"],
        "Payments::Payment.RecordPartialRefund" => &["confirmed_amount", "refund_transaction_id", "reported_processor"],
        "Payments::Payment.DeclineRefund" => &["reason", "reported_processor"],
        "Payments::Payment.OpenDispute" => &["dispute_id", "reason", "reported_processor"],
        "Payments::Payment.WinDispute" => &["reported_processor"],
        "Payments::Payment.Chargeback" => &["reported_processor"],
        _ => &[],
    }
}

pub const QUERIES: &[crate::kernel::QueryDef] = &[
crate::kernel::QueryDef {
    verb: "Payments::Payment.Status",
    aggregate: "Payments::Payment",
    conditions: &[
        crate::kernel::QueryCondition { field: "reference", comparator: crate::kernel::query_comparators::QueryComparator::Eq, value: crate::kernel::QueryConditionValue::Arg("reference") },
    ],
    reference_hop_conditions: &[

    ],
    order_by: None,
    offset: None,
    limit: None,
    authorization: None,
},
crate::kernel::QueryDef {
    verb: "Payments::Payment.Pending",
    aggregate: "Payments::Payment",
    conditions: &[
        crate::kernel::QueryCondition { field: "status", comparator: crate::kernel::query_comparators::QueryComparator::Eq, value: crate::kernel::QueryConditionValue::Literal("pending") },
    ],
    reference_hop_conditions: &[

    ],
    order_by: Some(crate::kernel::query_ordering::OrderBy { field: "reference", descending: false, nulls: crate::kernel::query_ordering::NullsMode::Native }),
    offset: None,
    limit: None,
    authorization: None,
},
crate::kernel::QueryDef {
    verb: "Payments::Payment.Succeeded",
    aggregate: "Payments::Payment",
    conditions: &[
        crate::kernel::QueryCondition { field: "status", comparator: crate::kernel::query_comparators::QueryComparator::Eq, value: crate::kernel::QueryConditionValue::Literal("succeeded") },
    ],
    reference_hop_conditions: &[

    ],
    order_by: Some(crate::kernel::query_ordering::OrderBy { field: "reference", descending: false, nulls: crate::kernel::query_ordering::NullsMode::Native }),
    offset: None,
    limit: None,
    authorization: None,
},
crate::kernel::QueryDef {
    verb: "Payments::Payment.Failed",
    aggregate: "Payments::Payment",
    conditions: &[
        crate::kernel::QueryCondition { field: "status", comparator: crate::kernel::query_comparators::QueryComparator::Eq, value: crate::kernel::QueryConditionValue::Literal("failed") },
    ],
    reference_hop_conditions: &[

    ],
    order_by: Some(crate::kernel::query_ordering::OrderBy { field: "reference", descending: false, nulls: crate::kernel::query_ordering::NullsMode::Native }),
    offset: None,
    limit: None,
    authorization: None,
},
];
/// `provides "authorization", assignments:` — the query `kernel::check_role_via` reads; `None` when no chapter here declares one.
pub const AUTHORIZATION_ASSIGNMENTS: Option<&str> = None;
/// Declared entity queries (`Aggregate.Entity.Query`) — `kernel::named_query::run_entity`.
pub const ENTITY_QUERIES: &[crate::kernel::named_query::EntityQueryDef] = &[
];

/// C3.7 for a named query's own arguments — `query_arg_checks`
/// (rust/project/queries.rb) has the full story.
pub fn check_query_args(verb: &str, args: &crate::kernel::Json) -> Result<(), crate::kernel::Refusal> {
    match verb {
        "Payments::Payment.Status" => {
            if let Some(x) = args.get("reference") { crate::generated::payments::payment::PaymentReference::from_json(&x.coerce_single_field("value"))?.check_invariants()?; }
            Ok(())
        }
        _ => Ok(()),
    }
}

pub const READ_MODELS: &[crate::kernel::read_model::ReadModelDef] = &[

];
